# post-merge：成功合并后（如 dev → uat/master），落在 uat/master 时自动剔除受保护配置文件（docs/ 不在此列，正常合并入库）。
# 若合并产生 modify/delete 冲突（hook 不会在冲突时运行），手动执行：
#   sh scripts/doc-tracking.sh untrack && git commit
_TOP="$(git rev-parse --show-toplevel)"
sh "$_TOP/scripts/doc-tracking.sh" untrack post-merge || true
