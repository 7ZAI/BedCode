# post-checkout：切到 uat/master 后剔除文档文件，并从 dev 恢复工作区副本。
# $3 == 1 表示分支级 checkout（文件级 checkout 跳过，避免噪音）。
[ "${3:-0}" = "1" ] || exit 0
_TOP="$(git rev-parse --show-toplevel)"
sh "$_TOP/scripts/doc-tracking.sh" untrack post-checkout || true
sh "$_TOP/scripts/doc-tracking.sh" restore || true
