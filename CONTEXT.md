# BedCode

BedCode 是局域网远程终端应用：桌面端作为主机运行终端会话，移动端作为远程终端控制它们，插件系统观察和扩展会话行为。本词汇表固定跨桌面/移动/插件使用的核心领域语言。

## Language

### 终端输入 (Terminal Input)

**提交输入行 (Submitted Line)**:
用户完成输入并提交的一条完整文本；`InputSubmitted` 事件的观察单位。由宿主从输入字节流重建（普通输入累积、回车触发提交），不是原始字节片段。
_Avoid_: 按键 (keystroke)、输入块 (input chunk)、「终端输入」（指一行时）

**InputSubmitted**:
提交输入行产生时触发的事件。属于纯观察通知：异步分发、无顺序保证，回调出错或超时不影响输入本身。宿主不做语义过滤——空提交（空行回车）同样触发，是否忽略由插件业务自行决定。对输入的修改/否决属于 `TerminalHandler`，不属于此事件。
_Avoid_: input hook、输入拦截器

**普通输入 (Plain Input)**:
可打印 Unicode 字符与空格；是唯一允许进入提交输入行的内容。
_Avoid_: normal data、原始输入

**控制字符 (Control Bytes)**:
混在输入字节流中的 C0 控制符（`\x00–\x1f`，含回车、Ctrl+C）、`\x7f` 及 ESC 转义序列；不是输入内容，一律丢弃。回车是提交触发器，不是内容。
_Avoid_: 「特殊键内容」（与 Special Key 冲突）

**特殊按键 (Special Key)**:
UI 工具栏主动发出的成型按键组合（`KeyCombo`，如 Ctrl+C 按钮、方向键按钮）；走独立的 `send_special_key` 通道。
_Avoid_: 控制字符、「快捷键」（指字节流中的按键时）

**快捷命令 (Quick Command)**:
移动端输入面板中可点击发送的单个命令项（命令文本 + 模式）。来源 = 命令预设（内置）或用户自定义（移动端本地存储）；点击行为由模式决定（见发送 / 执行）。
_Avoid_: 自定义命令（指内置预设项时）、快捷指令（协议层 QuickAction 未实装，避免混用）

**命令预设 (Command Preset)**:
按 Agent CLI 分组的内置快捷命令集（每套 12 条：新会话/换会话/skills/压缩/查看上下文 5 类必选 + 7 条该 CLI 高频命令）；内置移动端本地、随 App 版本演进；切换 Agent CLI 时直接覆盖面板命令列表，不合并用户数据。
_Avoid_: 快捷键预设（按键集全局一套，不分 agent）、命令集（泛称）

**发送 / 执行 (Send / Execute Mode)**:
快捷命令的两种点击行为。发送：命令文本发送到终端输入行、不带回车，供用户继续补全后自行提交（skills 类命令用）；执行：命令文本 + Enter 直接提交（其余命令）。
_Avoid_: 提交、回车（指执行时）

### 终端滚动 (Terminal Scrolling)

**TUI 应用 (TUI App)**:
进入终端备用屏幕、视口滚动由应用自身管理（而非终端 scrollback）的全屏交互式程序，如 opencode、vim、htop。移动端对其提供滚动兼容转发，与普通 CLI 的 scrollback 滚动行为不同。
_Avoid_: 全屏应用、交互式应用

**备用屏幕 (Alternate Screen)**:
终端中与主缓冲区互斥的显示区；TUI 应用在其上整体重绘，内容不进入终端 scrollback，故移动端无法用缓冲区滚动查看其历史。
_Avoid_: 全屏模式、TUI 屏幕

**应用内滚动 (In-app Scroll)**:
TUI 应用内部的视口移动，由应用自行维护；终端只负责把用户的滚动意图（SGR 滚轮事件）转发给应用。
_Avoid_: TUI 滚动（歧义）、内部滚动（泛称）

### 插件 (Plugin)

**插件契约 (Plugin Contract)**:
WIT 声明的插件与宿主双向能力边界：宿主能力 import 集 + 插件导出回调集。桌面端与移动端各持一份、随产品能力分化独立演进（如移动端刻意无 session 能力），同名词义保持对齐，传输机制（组件模型）两端一致。
_Avoid_: ABI（指契约内容时）、接口（泛称）

**启用 (Enabled)**:
用户对插件的持久化偏好，表示插件应随应用启动自动激活。Toggle 开关绑定此偏好，修改时立即联动激活/停用；插件上报错误时自动撤销（避免坏插件每次启动报错），用户重新启用即重试。
_Avoid_: 开启、开关状态、激活（指偏好时）

**激活 (Activated)**:
插件的运行时状态，表示 activate() 已成功执行、插件正在运行。由状态徽章展示，独立于启用偏好——已启用但激活失败的插件保持 Error 状态，插件可调用 report_ready 自愈回 Activated（不改启用偏好）。
_Avoid_: 启用（指运行时状态时）、激活状态（泛称）

**插件包 (Plugin Package)**:
插件的分发单元，单个 zip 文件（内含 plugin.json、index.js 与可选的 .wasm），由构建脚本从插件目录打包产出；端上安装（文件选择或 URL 下载）统一以插件包为单位解压安装。dev 场景使用未打包的插件目录（Dev Shell 直接加载源码）。
_Avoid_: 安装包、插件压缩包、artifact

**Dev Shell (开发环境)**:
SDK 内置的浏览器调试宿主：空壳前端 + 移动端（或桌面端）页面骨架 + mock PluginContext，直接加载插件源码（vite HMR）。覆盖前端 UI 与事件链路调试；WASM/Rust 后端命令、真实连接与系统能力不在浏览器运行，仍需真机验证。`bedcode-plugin dev` / `bedcode-plugin-desktop dev` 启动。
_Avoid_: 调试器、playground（指浏览器页面对宿主时）

**卸载 (Uninstall)**:
删除已安装插件的插件包文件（目录、存储、启用偏好一并清理），不可恢复。仅对用户安装的插件提供；内置插件只可停用。
_Avoid_: 删除、移除、停用（指卸载时）

**插件配置 (Plugin Configuration)**:
插件声明的可配置项集合（由 manifest 的 contributes.configuration 描述 schema），用户在插件详情页/配置页填写，值存于插件 storage 的统一键下。可缺失——插件可声明无任何可配置项，此时配置入口置灰。
_Avoid_: 设置项（与 Settings 扩展点混用时）、选项（泛称）

**扩展点 (Contribution / Extension Point)**:
插件向宿主声明的能力注入位（命令、视图、终端处理、文件处理器等）；同一 contributes 集合下并列多种，详情页统一展示插件贡献了哪些扩展点。新增/收减扩展点只需扩展点种类描述表加一条目，不动渲染分支。
_Avoid_: 贡献（中译泛称）、扩展（指单个注入项时）

**插件图标 (Plugin Icon)**:
插件 manifest 声明的标识图（图片路径或内联 SVG 插画），列表卡片/详情页 Hero 展示；缺省时按插件 id 生成渐变字母头像回退。
_Avoid_: logo（与品牌 logo 混淆）、头像（与用户头像混用时）

**插件详情页 (Plugin Detail)**:
单个插件的二级页面，展示 Hero、扩展点、权限、详细信息折叠区；配置在独立配置页，详情页提供入口按钮（仅激活且有配置项时可点）。与配置页是扁平两兄弟页，都直接返回列表页，非嵌套父子。
_Avoid_: 插件信息页、插件设置页（与配置页混用时）

### 认证与设备 (Authentication & Devices)

**认证方式 (Auth Method)**:
移动端连接桌面端时采用的认证途径，连接时选择。取值：配对码、QR 令牌、生物认证、会话令牌（JWT 有效期内重连）。
_Avoid_: 认证类型、登录方式

**配对码 (Pairing Code)**:
6 位数字、60 秒有效期的配对凭据，桌面端生成展示、移动端输入，完成首次配对并获发会话令牌。
_Avoid_: 验证码、PIN

**QR 令牌 (QR Token)**:
桌面端生成的单次有效二维码令牌，移动端扫码完成首次配对。与配对码是首次配对的两种等价通道。
_Avoid_: 二维码（指图片而非凭据）、扫码

**会话令牌 (Session Token)**:
认证成功后桌面端签发的 JWT（HS256，7 天有效）；移动端缓存，有效期内重连以 Reauthenticate 静默再认证，到期后重新走认证方式。
_Avoid_: token、access token、jwt（单独使用）

**生物认证 (Biometric Auth)**:
认证方式之一：移动端通过系统生物识别（指纹/面容）解锁设备本地私钥，对桌面端挑战值签名完成认证。体感零输入，与配对码并列、不取代。
_Avoid_: 指纹登录、面容解锁、免密登录

**生物认证绑定 (Biometric Enrollment)**:
把生物凭证注册到桌面端的过程；前提是已通过配对码/QR 建立连接，公钥经已认证连接上报，存于桌面端对应配对记录。可随时重新绑定/解绑。
_Avoid_: 录入生物特征、注册生物信息、绑定生物认证（指状态时）

**生物凭证 (Biometric Credential)**:
移动端本地的密钥对：私钥存于系统安全硬件（Android Keystore / iOS Secure Enclave），仅生物认证通过后可取用签名，永不出设备；公钥存于桌面端配对记录（pairings.public_key）。
_Avoid_: 证书、密钥对（泛称）、设备密钥（与设备本身无关）

**挑战值 (Challenge Nonce)**:
桌面端在生物认证握手中下发的一次性随机数，移动端签名后回传，防止重放攻击。
_Avoid_: nonce（单独使用）、挑战码

**设备 ID (Device ID)**:
移动端生成的稳定 UUID，随认证载荷上报；是桌面端配对记录的主键、连接历史的关联键。设备 ID 标识"这台手机上的身份"，与设备指纹用途不同。
_Avoid_: 设备标识（泛称）、pairing id

**设备指纹 (Device Fingerprint)**:
派生自设备硬件、用于识别"同一台设备"的标识；用于配对去重、在线状态匹配与 WS 会话关联，不参与密钥绑定。
_Avoid_: 设备 ID（两者用途不同）

**设备连接历史 (Connection History)**:
桌面端按设备记录的连接事件序列（成功/失败、认证方式、起止时间），以设备 ID 关联；移动端移除设备时一并删除（无审计需求）。
_Avoid_: 连接记录（泛称）、日志

**优先认证方式 (Preferred Auth Method)**:
移动端设置中记录的首选认证方式，连接时默认采用、可临时切换；生物认证未绑定时提示后临时降级为配对码。
_Avoid_: 默认认证、认证偏好

### 自动任务 (Auto Task)

**任务 (Task)**:
agent 会话内的一次执行轮次：以用户提交的输入行或队列注入的 prompt 为起点，到 agent 回答完成为止。任务只记录发起输入与常规信息（状态、起止时间、结果、执行 agent）；轮次内的问答、命令不产生独立任务记录（深挖对话细节经 JSONL 日志，按 agent 会话 ID 解析）。
_Avoid_: 对话、轮次（与"任务"指同一概念时的中文泛称）、子任务

**任务发起输入 (Task Prompt)**:
开启任务的那条输入内容，任务记录中唯一的任务内容字段。手动输入与队列/定时注入在任务记录上不区分内容形态，仅来源不同。
_Avoid_: 任务内容（泛称）、描述（指内容时）

**任务记录 (Task Record)**:
任务执行轮次的持久化档案行：承载发起输入、状态、来源、执行 agent、起止时间、结果与 token 用量等字段；队列项出队进入执行即产生记录，终态后转为档案。与任务队列是同一实体的不同状态视图。
_Avoid_: 任务日志、任务历史（指单个记录行时）

**命令 (Command)**:
以 `/` 开头的提交输入行（如 `/clear`、`/model`），属于 CLI 语义而非任务，不创建任务记录。当前一刀切过滤，未来预留白名单——以 `/skills` 等开头的"任务型斜杠命令"可放行。
_Avoid_: 斜杠命令、指令

**执行 agent (Executing Agent)**:
执行任务的 agent CLI 身份（claude / codex / opencode / pi），会话创建时由启动命令检测得出，写入任务记录。粒度到 CLI，不细分 subagent 类型或模型名。与移动端命令预设的加载键（Agent CLI）概念独立，互不依赖。
_Avoid_: 模型、Agent CLI（指移动端命令预设加载键时）

**Agent CLI**:
移动端输入面板命令预设的加载键（claude_code / pi / codex / opencode / generic）。移动端对会话配置的启动命令（command 字段）做关键词检测识别（claude/codex/opencode/pi），识别结果与用户手动覆盖存移动端本地 JSON（按会话配置 id 映射）；未识别（generic）不加载预设。与执行 agent（任务记录中的最终身份，桌面端启动命令检测）可同源但各属一端，互不依赖。
_Avoid_: agent 类型、执行 agent（指桌面端任务记录时）

**常规自动任务 (Regular Auto Task)**:
面向运行中会话的队列化自动执行：任务加入会话队列，当前任务终态后自动出队执行，队列非空时联动自动授权。是任务队列的调度语义，与定时自动任务并列。
_Avoid_: 自动执行、队列任务（指整个机制时）

**定时自动任务 (Scheduled Auto Task)**:
指定触发时刻、一次性执行的自动任务：携带会话配置与一组 prompt，触发时新建会话并依次执行。错过触发时刻（应用关闭）不补跑，标记 missed；创建会话失败记为执行失败。
_Avoid_: 定时器、job（单独使用）、定时任务（与常规自动任务混淆时）

**计划任务 (Task Scheduler)**（插件 com.bedcode.scheduler）:
独立的通用调度框架（仅桌面端）：以 cron 表达式（6 段含秒、本地时区）周期触发，执行 shell 脚本或内联命令，与 agent 完全无关。通过 CLI（bedtask）管理，CLI 随插件包分发、由插件控制安装/卸载。触发在桌面端进程内（窗口关闭 = 托盘驻留，调度继续；真正退出后错过不补跑）。桌面端关闭期间到期的周期任务标 missed 但任务本身继续排下一周期。
_Avoid_: 定时任务（与定时自动任务混淆时）、计划任务服务（指 Windows OS 的计划任务时）

**调度任务 (Scheduled Task)**:
计划任务插件中的单条任务条目：由 cron 表达式 + 执行内容（脚本路径或内联命令）+ 执行环境（cwd/env/超时）+ 启停开关组成。执行记录（job_executions）与任务定义分离：每次触发生成一条执行记录（succeeded / failed / timeout / missed），任务定义持续存活至删除或停用。
_Avoid_: job（单独使用）、定时任务（与定时自动任务混淆时）

**任务队列 (Task Queue)**:
会话维度的待执行任务集合（pending 任务按序出队）。队列项即"尚未进入执行的任务"，与任务记录是同一实体的不同状态视图。
_Avoid_: 排队、待办

**预设任务 (Preset Task)**:
移动端本地保存的任务模板，供一键发送到终端或加入任务队列。属移动端本地概念，重置/卸载即清空；与桌面端「预存任务」完全独立、互不干扰。
_Avoid_: 预存任务（指桌面端概念时）、快捷指令

**预存任务 (Stored Preset)**:
桌面端 auto-task 插件的任务模板：创建后加入任务队列即消耗删除，一次性使用。与移动端「预设任务」独立，二者数据与生命周期互不相通。
_Avoid_: 预设任务（指移动端概念时）

**可重复 (Repeatable)**:
预设任务创建时设定的属性：可重复任务不受执行历史限制，可反复加入任务队列；不可重复任务一旦执行过即锁定，不能再加入。同一时刻队列中至多一个实例，对两类任务统一生效。
_Avoid_: 一次性（指执行次数时）

**已执行 (Executed)**:
预设任务被加入任务队列或手动执行成功即视为已执行。这是用户本地操作的可靠记录，而非服务端任务终态的通知；锁定判定以此为准，不依赖可能丢失的通知。
_Avoid_: 已完成（指收到完成通知的子态时）

**执行状态 (Execution Status)**:
预设任务的生命周期状态：未使用 → 执行中 → 已完成 / 已中断。已完成与已中断同属已执行，对不可重复任务均锁定、无重试路径；编辑任务内容或从队列移除该任务是仅有的解锁路径。
_Avoid_: 任务状态（指队列项/任务记录时）

**上下文清理 (Context Clear)**:
自动任务（队列出队/定时触发）执行前先执行的 agent 上下文清理命令，用于防止上下文超限。清理命令由执行 agent 的 profile 提供（Claude Code 为 `/clear`，其他 agent 走扩展点）。清理可能导致 agent 会话重建（新 agent 会话 ID 与新的 session_mapping），其触发的会话结束事件属于机制副作用，不得改写前一任务的终态；全新会话的首个任务无需清理。
_Avoid_: 清屏、重置对话、/clear（指机制时）

### AI 对话 (AI Chatbox)

**对话 (Conversation)**:
AI Chatbox 插件的会话单元：由元数据（标题、模型、供应商、system prompt）与消息序列组成，持久化为数据目录下 `conversations/{convId}.jsonl` 的一个文件。对话级 system prompt 与模型切换只影响该对话后续消息。
_Avoid_: 会话（与宿主终端会话 Session 混淆时）、聊天记录（指存储时）

**对话日志 (Conversation Log)**:
对话历史的唯一存储形态：JSONL 文件（首行 meta、后续逐行 message），明文可查、可备份、可手工编辑；取代 v1 的宿主 SQLite 存储。追加写入为主（重生成时覆盖文件末尾的 assistant 行），删除对话 = 删除文件 + 从索引移除。
_Avoid_: 数据库、存储表

**数据目录 (Data Directory)**:
插件数据落盘的根目录（桌面 `{HomeDir}/.bedcode/ai-chatbox/`、移动 `{AppDownloadsDir}/ai-chatbox/`），位于插件包目录之外——卸载插件不清用户数据。内含 `index.jsonl`（对话索引）、`providers.json`（供应商配置占位）与 `conversations/`。
_Avoid_: 插件目录、工作目录

**目录授权 (Directory Authorization)**:
宿主 fs_auth 弹窗机制对插件数据目录的授权：插件 activate 时一次弹窗批量申请数据目录前缀，同意后前缀制持久化放行后续读写；拒绝或 30 秒超时则激活失败（Error 状态），重新启用可再次弹窗。撤销授权后插件写文件失败并提示「目录授权已失效」。
_Avoid_: 文件权限（指操作系统权限时）、授权弹窗（单指 UI 时）

**流式回复 (Streaming Reply)**:
模型回复逐字（chunk）渲染的生成过程，可中途停止；停止/失败时已接收内容照常落盘。发送后可「重新生成」重跑最后一条用户消息，落盘覆盖旧回复行防重启后旧回复复现。
_Avoid_: 打字机效果、流式输出（指网络传输时）

**token 用量 (Usage)**:
每条助手消息的 prompt/completion/total token 计数，来自宿主 SSE done 事件透传的 usage 字段，随消息落盘并显示在消息 meta 行。
_Avoid_: 消耗、计费（指金额时）

**供应商 (Provider)**:
一个已配置、可用的模型服务实例（配置页列表中的每个条目，含名称、baseUrl、API Key、模型列表、协议方言）。由预设模板创建或空白自定义，保存后持久化。
_Avoid_: 自定义供应商、预设供应商（旧词把模板和实例混为一谈）

**协议方言 (API Style)**:
供应商适配层的分派键，决定该供应商请求构建与流式响应解析所用的 API 方言（openai / anthropic / gemini / custom）；存储于供应商配置，缺失时按 openai 处理。custom 为私有网关预留逃生舱槽位，本期不实现 UI。
_Avoid_: 协议类型、API 格式（指方言时）、接入方式（泛称）

**预设模板 (Preset Template)**:
内置添加起点（DeepSeek / 通义千问 / OpenAI / Anthropic），只读、不进入供应商列表、不可删除；添加流程中选择后回填表单（名称/baseUrl/模型），用户只需填 API Key。
_Avoid_: 预设目录、预设供应商

**激活供应商 (Active Provider)**:
当前对话正在使用的供应商，列表行内以圆点标记；切换即生效，删除激活供应商后自动回退到列表第一个。
_Avoid_: 默认供应商、当前供应商

**自定义 (Custom)**:
添加流程中的空白模板选项：默认图标 + 「自定义」标签，表单全空，供中转站/私有网关等非内置服务接入。
_Avoid_: 自定义供应商（指列表分区名时）

### 对等网络 (Peer Network)

**对等网络 (Peer Network)**:
宿主两端内置的通用设备互联层：任意 BedCode 节点（桌面/移动）在同一网络内互相发现并建立直连、双向传输。无中心协调者，文件传输是首个上层消费者，后续能力（投送、同步等）挂载其上。
_Avoid_: 局域网传输（泛称）、P2P 网络（指具体协议栈时）

**对等节点 (Peer)**:
接入对等网络的 BedCode 设备实例。桌面端与移动端在节点语义上同构、无角色差异：每个节点既是服务端（监听、接受连接）又是客户端（发起、浏览、下载）。
_Avoid_: 主机（指对等网络语境时）、服务器、客户端（指节点角色时）

**直连 (Direct Connection)**:
两个节点间不经任何中间节点转发的连接。对等网络只服务直连可达的对端，不做中继路由；跨子网/AP 隔离导致不可达的对端不在连接范围内。
_Avoid_: 中继、跳板转发、路由（指数据转发时）

**节点身份 (Peer Identity)**:
节点首次启动生成长效密钥对，节点 ID 即公钥指纹，跨重启稳定、重装 App 即重生为新身份。发现列表以「设备名 + 短指纹」呈现，防冒充。与终端配对体系的设备 ID（移动端 UUID）是两个概念，迁移后可能统一。
_Avoid_: 设备 ID（指终端配对体系时）、账号

**可信对端 (Trusted Peer)**:
与本节点完成过首次连接确认的对端节点。信任关系逐对、双向、持久（本地存对方节点 ID），设置中可撤销、撤销后重连需再确认。可信对端静默重连，可浏览我暴露的共享目录。未建立信任的已发现节点仅可见、不可交互——不能发传输请求、不能浏览。已有终端配对关系的设备对首次对等接触免确认直接建立信任（终端配对记录即同一用户的证明）。
_Avoid_: 已配对设备（指终端配对体系时）、好友、白名单（指接收策略自动接受时）

**首次连接确认 (First-connect Confirmation)**:
陌生节点发起连接时被连接方收到的应用内一次性确认弹窗：同意即建立信任关系，拒绝则本次连接失败（可稍后重试）；此后两节点间连接不再弹窗。仅应用内弹窗，不强制扫码。
_Avoid_: 配对（指终端配对码流程时）、授权弹窗（指宿主 fs_auth 弹窗时）

**节点发现 (Peer Discovery)**:
对等网络专用的双向 mDNS 广播与扫描：每个节点既广播自身也浏览他人，与终端链路的 mDNS 发现并存、互不感知。记录携带设备名、协议版本与能力通告。Android 收多播依赖 MulticastLock，跨子网/AP 隔离下天然不可见。
_Avoid_: 扫描（指用户界面动作时）、终端发现（指 _bedcode._tcp 那套时）

**在线 (Presence)**:
节点的可见性状态：发现记录在网内可被扫到即为在线，与本机是否维持着到它的连接无关。连接按需建立——点击连接或发起传输时才握手，空闲超时即断开；节点列表来自发现缓存，不等于活跃连接列表。
_Avoid_: 已连接（指在线状态时）、活跃节点（泛称）

**能力通告 (Capability Advertisement)**:
发现记录中声明本节点可用上层能力的字段（文件传输为首个能力位）。发现列表据此展示对方「是否具备同样能力」；能力集合随上层功能的启停变化，不随节点身份变化。
_Avoid_: 插件列表（指具体实现载体时）、服务声明

### 文件传输 (File Transfer)

**共享目录 (Shared Directory)**:
节点向可信对端只读暴露的目录集合，两端对称同构：移动端条目以 SAF URI（`content://tree/...`）存储，经系统目录选择器选择并持久化授权（重启后仍有效），app 私有下载目录是唯一的免授权真实路径特殊条目；桌面端条目为用户选择的文件夹。仅可信对端可浏览与拉取，任何方向均不可写入。
_Avoid_: 授权目录（泛称）、下载目录

**下载目录 (Download Directory)**:
节点接收文件的统一落点，两端对称：移动端默认写入 MediaStore.Downloads（公共下载目录，API 29+ 零权限、系统文件管理器可见），失败回退 app 私有下载目录；桌面端为可配置文件夹（缺省 `Downloads\BedCode\`）。本机主动下载与对端推送均落此目录，逐次不弹窗。与共享目录职责分离——共享只读暴露、下载定向接收。
_Avoid_: 接收目录、保存目录（与「保存到…」单文件操作混用时）

**发送 (Send)**:
我发起的传输任务。队列按发起方分为「正在发送 / 正在接收」两组；发送侧任务从排队即建立记录。上传 (upload) 保留为协议层传输方向词，不用于用户可见分类。
_Avoid_: 上传（指用户可见分类时）、推送（单字）

**执行方 (Transfer Executor)**:
负责实际搬运字节流的一端。两端对称、均可担任：推（发送方写向接收方，upload 语义）或拉（接收方从发送方读，download 语义）。断点真源恒为落盘方（接收端）已写字节。
_Avoid_: 传输端（描述性）、服务器端（旧中心模型残留）、数据驱动者（旧形态）
_Avoid_: 上传（指用户可见分类时）、推送（单字）

**接收 (Receive)**:
对端发起的传输任务（对端上传文件到本机）。接收端自对端发起上传请求起即建立任务记录，进入「正在接收」分类，可取消/拒绝；放行与否受接收策略约束。与发送的区分维度是发起方，不是协议方向。
_Avoid_: 接受（旧称，用户定稿为接收）、被推送（描述性说法）、被动传输

**传输批 (Transfer Batch)**:
一次「发送」动作对应的文件集合（传输与历史记录的基本单位）。发送方为一次发送生成批 ID，接收策略为每次询问时先向接收方整批征询，批准后批内文件免询问逐条传输；批状态（pending/running 与终态）由接收端持有并同步发送方。传输与历史记录均为批粒度（一批 = 一条记录）。
_Avoid_: 批次（泛称）、请求组（描述性说法）

**扇出发送 (Fan-out Send)**:
一次发送动作指定多个接收方的用户操作。领域模型中落地为多条相互独立的传输批——各批独立批 ID、独立进度、独立过各自接收方的接收策略闸门，任一接收方拒绝不影响其他批次；「群发」仅是界面编排概念，不存在组会话实体。
_Avoid_: 群发会话、广播分发、组传输

**接收策略 (Receiving Policy)**:
接收端的全局单开关（宿主持久化设置）：每次询问（默认）/ 直接接收 / 直接拒绝。每次询问需用户以「接受全部 / 拒绝全部」应答整批传输请求、超时自动拒绝；直接接收与直接拒绝无需应答。与信任层正交——信任决定谁有权向我发起传输，接收策略决定收到后放不放行；全局生效、不区分对端。
_Avoid_: 接受策略（旧称）、上传策略、白名单（指 per-peer 自动接受时）

**传输请求 (Transfer Request)**:
每次询问模式下，发送方向接收方提出的整批传输征询（批 ID + 文件清单 + 总大小），接收方一次应答（接受全部 / 拒绝全部），超时自动拒绝；批准后批内文件即开始逐个传输。
_Avoid_: 批量请求（描述性）、上传请求（旧称）

**传输历史 (Transfer History)**:
两端各自保存的全部终态传输档案（发送 + 接收、完成/失败/被拒/取消），按批记档、滚动淘汰、可一键清空；直接拒绝模式下未建立批记录的请求不记入。
_Avoid_: 传输记忆（描述性）、传输日志（泛称）

### 远程隧道 (Remote Tunnel)

**中继 (Relay)**:
用户自购云服务器上运行的公网转发服务，移动端与桌面端均以出站连接接入、经其中转建立隧道。视为可信节点（运营者即用户本人），传输层 TLS 在中继终止。
_Avoid_: 服务器（指转发服务时）、云主机、隧道（指服务本身时）

**隧道 (Tunnel)**:
移动端与桌面端之间经中继建立的逻辑连接，承载全部业务流量（WebSocket + HTTP）；对上层协议透明，表现为字节管道。
_Avoid_: 内网穿透（指具体连接时）、通道（泛称）

**隧道 ID (Tunnel ID)**:
中继为每条隧道分配的 128-bit 高熵标识，即公网地址的路径段（`wss://relay/t/{id}`）；移动端凭它接入隧道，未知 ID 的连接在中继处即被拒绝。
_Avoid_: token、隧道密钥（与共享密钥混淆）、穿透码

**首次配对 (First-time Pairing)**:
新设备的初次认证流程（配对码 / QR 令牌），仅允许来自局域网的连接执行；经隧道（公网）的连接一律拒绝此流程，只放行已配对设备的认证（JWT 续期 / 生物认证）。
_Avoid_: 远程配对（隧道内不存在首次配对路径）

### 文字识别 (OCR)

**OCR 插件 (OCR Plugin)**:
移动端插件，负责取图入口与识别结果展示（UI/编排层，WASM）；自身不携带识别引擎，识别经宿主命令委托给宿主 OCR 引擎。
_Avoid_: OCR 引擎、识别器（指插件自身时）

**宿主 OCR 引擎 (Host OCR Engine)**:
移动端宿主 Rust 侧的原生识别引擎（PaddleOCR PP-OCRv4 ONNX 模型 + ort 运行时），设备本地推理，加载一次后复用；随 App 发版演进，不随插件 zip 分发。插件经宿主命令（如识别图片、查询引擎状态）调用。
_Avoid_: 本地 OCR、插件后端（宿主引擎不是插件的一部分）

**离线识别 (Offline Recognition)**:
完全在设备本地完成的文字识别，无网络依赖；与在线识别（云端 API，v2）相对。
_Avoid_: 本地识别（与「设备本地」歧义不大但语义不完整）、本地推理（指用户可见行为时）

**文本行 (Text Line)**:
识别结果的最小可读单元：一段文本 + 原图坐标系位置框（bbox）+ 置信度；结果页以行列表呈现，支持复制全文与行级点击复制。引擎按阅读顺序（自上而下、行内从左到右）输出。
_Avoid_: 识别块、文字块（指单行结果时）、段落（文本行不保证段落聚合）

### 磁盘清理 (Disk Cleanup)

**规则清单 (Rule Catalog)**:
磁盘清理插件内置的声明式清理目标全集，实际删除什么的唯一真源。预打开授权（白名单）只是能力上限，不决定删什么；清单随插件版本演进，用户只能逐项目勾选、不能自行编辑条目。
_Avoid_: 规则库、清理规则（泛称）、配置项（指清理目标时）

**清理目标 (Clean Target)**:
规则清单中的单个条目：一个缓存类别（如浏览器缓存、崩溃转储）+ 匹配模式 + 展示文案；扫描结果分组与用户勾选的最小单位。
_Avoid_: 清理项、规则（单独使用）

**硬黑名单 (Hard Blacklist)**:
永不删除的文件名单（浏览器 Cookies / 登录数据 / 书签、应用配置与本地数据库等），优先级高于任何规则模式：即使被某条清理目标的模式命中也强制跳过。与规则清单构成双层防御——清单管「删什么」，黑名单管「绝不删什么」。
_Avoid_: 排除列表（泛称）、忽略名单

**需系统权限项 (Elevated Target)**:
需要管理员提权才能删除的系统级残留（如 Windows Update 缓存、系统临时区）。当前版本置灰展示、不可勾选，不提供提权执行路径。
_Avoid_: 管理员项、系统项（泛称）

**隔离区 (Quarantine)**:
删除的前置暂存区：清理删除一律先移入隔离区、绝不直接销毁；保留期固定 7 天，过期后惰性清除（激活或下次扫描时顺手清，无常驻任务）。是「删错可挽回」承诺的机制载体；二期注册表清理的备份→还原闭环必须复用同一机制。
_Avoid_: 回收站（指系统回收站时）、垃圾箱、备份区（指二期注册表备份语义时）

**清理批次 (Clean Batch)**:
一次「清理」动作移入隔离区的文件集合，附带类别 / 文件数 / 空间的元数据记录；还原以批次为单位（整批一键还原），不做单文件级挑选。与文件传输的传输批同构不同域。
_Avoid_: 批次（泛称）
