# Dependencies
**/node_modules/
.pnpm-store/
# 项目已迁移 pnpm（各工程维护 pnpm-lock.yaml）；这里的 package-lock.json 只防误用 npm 生成新锁文件
**/package-lock.json
.playwright-cli

# Husky v9 生成的钩子运行器（由 `husky` / `pnpm install` 重新生成，不入库）
.husky/_/

# Build outputs
**/dist/
build/
*.local

# Documentation
# 注意：只忽略仓库根 docs/，子目录 docs（如 bedcode-desktop/docs/、bedcode-mobile/docs/）允许入库——
# AGENTS.md "Code Map" 段要求两端 code-map.md 作为必读入口。CLAUDE.md / CONTEXT.md / .scratch/ /
# 顶层 .pi/ 受 doc-tracking.sh（scripts/doc-tracking.sh）分支级控制：dev 与 feature/* 全分支跟踪，
# uat / master 不入库（git rm --cached 剔除、工作区保留）。脱离钩子场景（如手动 git add）需自行注意。
/docs/

CLAUDE.md
CONTEXT.md


# Tauri / Rust
**/target/
**/src-tauri/**/Cargo.lock
**/src-tauri/bedcode.db
**/src-tauri/gen/android/app/src/main/assets/_up_/
*.pdb
# Python 编译缓存（通用；过去曾针对 auto-task 脚本硬编码，现统一）
**/__pycache__/
*.pyc
*.pyo

# 插件编译产物（WASM / index.js / plugin.json）由各插件 build 脚本生成，不入库
**/src-tauri/resources/plugins/
**/src-tauri/gen/android/app/src/main/assets/resources/plugins/
# OCR 模型在 gen/android assets 的拷贝（tauri build 产物；真源在 src-tauri/resources/ocr_models/ 入库）
**/src-tauri/gen/android/app/src/main/assets/resources/ocr_models/

# IDE and editors
.idea/

# AI 工具配置跟踪策略：关键配置入库（subagent / hook / 命令 prompt），
# 运行时产物（运行日志 / 本机 bridge / Python cache / 个人 worktree）不入库。
# .opencode/ 整目录不由根 .gitignore 忽略，由 .opencode/.gitignore 自身接管（已 ignore node_modules / lock / 自身）。
# 关键配置：subagent 定义与 hooks。运行时产物（settings.local.json / *.log.* / __pycache__ / skills bridge / worktrees）另由下方规则忽略。
.claude/*
!.claude/agents/
!.claude/agents/**
.codex/*
!.codex/hooks.json

.omp/
.pi/

.VSCodeCounter/
*.swp
*.swo
*.sublime-workspace
*.sublime-project
*.iml
# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
*.stackdump

# Logs
logs/
.dev-logs/
*.log

# Dev Shell 调试脚本（临时产物，不入库）
bedcode-desktop/packages/plugin-sdk-desktop/dev-shell/debug-*.mjs
# 终端 PTY 输出 dump 调试数据（临时产物，不入库）：
# pty/terminal 字节对比 dump .bin 由排查临时功能产生（见 SCRATCHPAD），
# 体积大（可达数十 MB）且排查完删除，禁止入库；terminal-mock 为调试用 mock
# 终端，整目录均不入库
bedcode-desktop/src/dev/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/
playwright/.cache/

# Environment files (important: never commit secrets)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# Cache directories
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
*.tsbuildinfo

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup

# Claude Code project files
.claude/settings.json
.claude/settings.local.json
.claude/*.log.*
.claude/auto_task_hook.py

# Pi session files (local, not for commit)
.pi/sessions/

# Superpowers temporary files (brainstorm state, etc.)
.superpowers/brainstorm/
.superpowers/skills/_draft/

# Git worktrees (isolated development workspaces)
.worktrees/

# Playwright MCP temporary files
.playwright-mcp/

# Misc
*.pem
*.p12
*.pfx

# Android/iOS signing keys (NEVER commit these!)
*.keystore
*.jks
key.properties
release.keystore

# Firecrawl web-scrape cache
.firecrawl/

# vite config 打包临时产物（崩溃/中断时残留）
**/vite.config.ts.timestamp-*.mjs
