[package]
name = "bedtask"
version = "1.0.0-beta"
description = "BedCode Task Scheduler CLI — manage scheduled tasks via localhost gateway"
authors = ["BedCode"]
edition = "2021"

[[bin]]
name = "bedtask"
path = "src/main.rs"

[dependencies]
serde_json = "1"
