{
  "name": "@bedcode/plugin-scheduler",
  "version": "1.0.0-beta",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frontend": "vite build",
    "build:rust": "cargo build --target wasm32-unknown-unknown --no-default-features --features wasm --manifest-path rust/Cargo.toml --release",
    "build": "node scripts/build.js",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@bedcode/plugin-sdk-desktop": "file:../../packages/plugin-sdk-desktop",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "terser": "^5.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
