# 08 — 物理滚轮平滑滚动（作废：被 07 维持关闭 gate）

**What to build:** 仅当 07 决策为「引入」时实施——滚轮分类器区分物理滚轮/触控板，仅对物理滚轮开启 `smoothScrollDuration=125`，触控板保持即时滚动防跟手延迟。若 07 结论为「维持关闭」则本票作废。

**Blocked by:** 07

**Status:** resolved（作废 — 07 决策维持关闭，07 已答复见其 Answer 段）

- [ ] 仅当 07 决策为「引入」时实施；否则关闭本票
- [ ] 滚轮分类器区分物理滚轮/触控板，纯类单测（Seam A）
- [ ] 仅物理滚轮开启 `smoothScrollDuration=125`，触控板保持即时
- [ ] WebGL 滚动重影回归不复发
