# 14: 配对 / 已配对设备 / 连接历史进插件 + 设置页配对分组退役

**父规格:** `.scratch/2026-09-19-terminal-session-plugin/spec.md` D6 / D8-P3

**What to build:** 设备域三块页面改由插件贡献的侧边栏目录渲染（位置与外观不变），配对码展示与倒计时、QR 生成、设备撤销确认都在插件里；宿主设置页的「配对」分组消失，改由插件贡献的设置分组承载 TTL、有效期、会话默认与后续任务默认项。设置项写入仍经插件后端由宿主原语仲裁。

**Blocked by:** 01, 05, 11, 13（分组扩展点、认证与设备语义、派生视图真源、会话视图先落地以统一 i18n 与排序口径）

**Status:** done（2026-09-20；桌面 `cargo test --no-fail-fast` lib **1061/0** + 8 集成目标 + doc、插件 crate **153/0**、SDK **85/0**；桌面前端 **259 files / 759 tests**、根 eslint **0 error / 127 warning**（零新增）；实施记录与门禁取证见 Comments。并行线（票 15 任务域搬迁）在同一工作树实施，本票的 lib.rs 补丁已复核在位）

- [x] 配对页 / 已配对设备 / 连接历史三块视图搬入插件前端并注册为侧边栏目录；宿主内置设备入口让位为跳转壳
- [x] 宿主设置页配对分组组件删除；插件贡献等价分组，字段与今天一致（配对码 TTL、QR 有效期），排序落在设置页既有分组序列中不突兀
- [x] 两层配置口径落地：标量项走既有声明式插件配置面，需交互控件的（配对码倒计时、QR、设备撤销确认）走组件式分组
- [x] 校验不降级：分组内写入路径经插件后端到宿主原语权限门；宿主原语层保留 TTL 边界等最终校验
- [x] i18n：`pairing.*` 命名空间落定；宿主 `desktop.device.*` / `settings.pairing.*` 中被搬走的 key 同步清理，zh-CN / en 双文件
- [x] 前端配对流程集成测试接缝不变（仍打在宿主薄转发命令门面上），不改断言即过
- [x] 桌面 `pnpm run test:run` + `pnpm exec eslint .` 0 error；`frontend-styles` 自查通过

## Comments

### 2026-09-20 实施落地

**并行说明**：实施期间并行线（pi agent）正在同一工作树实施票 15（任务域后端搬迁），
与票 14 在 `plugins/session/rust/src/lib.rs`、`plugins/session/plugin.json` 上重叠。
按 AGENTS「并行 agent 冲突纪律」，本票按「零重叠优先」的顺序推进：插件前端 → 宿主前端
→ WIT / SDK / host_impl（并行线不触）→ 最后以最小补丁落 lib.rs（mod 声明 + 命令面分支），
收尾复核补丁是否被覆盖。

**0. 顺带修正的既有缺陷（票 13 遗留的真实运行缺陷）**

- **插件 manifest 从未声明 `ui:sidebar`**：票 13 起插件运行期注册侧边栏目录，但
  `plugin.json.permissions` 只有 `auth / peer / session:config / session:read /
  session:write / storage`。宿主前端 `PluginContext.ui.registerSidebarPanel` 经
  `requirePermission('ui.registerSidebarPanel')` 快速失败 → **真实环境下插件激活即抛错**
  （票 13 的单测全 mock context，未覆盖该路径）。四家既有插件（auto-task / agent-hub /
  file-transfer / ai-chatbox）均声明该权限，属遗漏而非设计。
  本票一次性补齐 `ui:sidebar` 与新增的 `ui:settings`，并同步插件 Rust 契约用例与
  插件前端 contract 用例两处 pin（宿主产物生命周期用例在 Rust 收集阶段同步）。

**1. 插件前端（票 14 主体，全部为宿主原页的逐类搬运）**

| 新增文件 | 来源 | 说明 |
| --- | --- | --- |
| `components/DeviceCenterView.vue` | 宿主 `DevicesView.vue` | Tab 配对 / 设备列表、网络信息条、配对码卡、QR 卡、在线/离线设备卡、撤销确认 |
| `components/ConnectionHistoryView.vue` | 宿主 `ConnectionHistoryView.vue` | 日期分组、统计、清空确认、设备下拉（无 `?deviceId=` 时默认第一台） |
| `components/PairingSettingsSection.vue` | 宿主 `SettingsPairingSection.vue` 正文 | 两项有效期（QR / 配对码），范围收敛 60–3600，最终仲裁在宿主原语 |
| `composables/useDeviceCenter.ts` | 宿主 `usePairing` / `useQrCode` / `useNetwork` / `useConnectedDevices` 编排 | 配对码 / QR / 设备列表 / 网络信息 / 宿主事件订阅（deactivate 与卸载统一释放） |
| `composables/useConnectionHistory.ts` | 宿主 `ConnectionHistoryView` 内联逻辑 | 分组 / 统计 / 方式与结果文案映射（唯一映射点） |

- `components/SessionModal.vue` → **`PluginModal.vue`**：弹窗外壳与业务域无关，设备页与会话页
  共用一份（避免第二份弹窗副本；票 13 的 4 处引用同步改名）。
- `index.ts`：注册三目录 —— 设备与配对 `order 100`（顶替宿主内置设备入口）/
  连接历史 `order 101`（紧随其后）/ 终端会话 `order 200`；设置分组 `order 200`
  （顶替退役的宿主内置配对分组）。
- i18n：新增 `pairing.*` 命名空间 68 key（zh-CN / en 双文件；文案逐字复制宿主
  `desktop.device.*` / `settings.pairing.*` / `common.*`，保证界面不变）。
- `devMock.ts` 增 `pairing` 域种子（设备 / 网络 / 配对码 / QR / 历史 / TTL），与测试数据同源。
- 取数红线（spec D2）复核：插件前端零 `@/` 宿主导入、零 `invoke(` 直调；导航走 SDK
  `getRouter()`（共享运行时 `__BEDCODE_SHARED__.router`），不引宿主 router 模块。

**2. 宿主侧**

- 删除 `views/DevicesView.vue`、`views/ConnectionHistoryView.vue`；新增
  `views/DevicesFallbackView.vue`（**最小壳**：兜底说明 + 已配对设备列表 + 撤销），
  `/devices` 与 `/devices/:id/history` 均指向它（深链不 404）。
- `useSettingsSections`：内置 `pairing` 分组退役（`BUILTIN_SECTION_ORDERS.pairing = 200`
  槽位保留不复用，防第三方撞位）；删除 `components/settings/SettingsPairingSection.vue`。
- **默认端口的落点**：随配对分组退役迁入 `SettingsSystemSection`（文案不变）。理由：端口是
  宿主服务器引擎配置（ADR 0022 裁剪线之外的宿主设置），插件无写入宿主 `AppConfig` 的原语，
  为一项引擎配置新开写原语不划算。**这是本票唯一的用户可见位置变化**（设置页第 2 组 → 第 5 组）。
- locales：`desktop.device.*` 51 项清理（保留兜底壳所需 6 项 + 新增 `fallbackNotice`）、
  `settings.pairing.*` 整体退役、新增 `settings.system.defaultPort(.Desc)`；zh-CN / en 同步。
- 宿主测试：删 `DevicesView.test.ts`；新增 `DevicesFallbackView.test.ts`（4）；
  `pairing-flow.test.ts` **收敛为「宿主命令门面集成」**（生成 / 取消 / 恢复配对码、
  设备列表、撤销设备；断言仍打在宿主命令名与入参上，接缝不变）——视图侧流程断言
  （展示与倒计时、事件驱动状态流转）随视图迁入插件工程 `deviceCenter.test.ts`。

**3. 契约与宿主原语（全部函数级追加，ABI 不 bump，desktop 仍 19）**

- WIT：`host-platform.local-ipv4-addresses`（本机可访问 IPv4，**空数组合法**——与
  `wsl-distros` 的显性报错口径不同，地址清单为空是正常状态）、
  `host-auth.connection-history-clear`（显式清空历史，与撤销配对的连带删除区分；
  **不影响配对状态**）。
- SDK：`HostPlatform::platform_local_ipv4_addresses`、`HostAuth::auth_connection_history_clear`、
  `ConfigKey::{PairingCodeTtl, QrTokenTtl}`（**读 host-config、写 host-auth，两处键白名单
  同形**），`wasm_host.rs` 三处实现接线。
- host_impl：`platform_local_ipv4_addresses`（`filter_ipv4` 纯函数 + `spawn_blocking`）、
  `auth_connection_history_clear`（**先计数再删除**——`delete_connection_history` 不返回
  行数，命中与否必须由此判定，否则「重复清空」与「从未有过历史」不可区分）、
  `config_get` 加 host_ctx 并补两键读侧（缺失 / 非法回退宿主默认值，与宿主命令面同口径）。
- 插件 Rust：新增 `device_face.rs`（网络信息 / 配对码 / QR / 设备列表 / 连接历史 /
  有效期设置六组命令面实现；`paired_list` 只回活跃记录并按 `pairedAt` 倒序——
  过滤与排序是插件的展示组织）+ `lib.rs` 命令面 13 条（**均为前端取数命令，
  不是互调 api，故 manifest `api` 面不变，仍 21 项**）。

**4. 关键设计取舍（实现期定）**

1. **QR 载荷形状零改动**：still `{host, port, token}` 三要素；`host` 由插件前端选择并落
   **插件存储** `pairing.qrHost`（宿主原为 `AppConfig.network.qr_host`）——移动端扫码逻辑
   与线协议形状不受影响（spec D1 自守边界）。
2. **设备页在线判定沿用事件指纹集合**（`device-connected` / `device-disconnected` 经
   `context.events.on` 订阅，宿主 `pluginEvents` 已桥接 Tauri `listen`），与宿主原页同语义；
   `devices-connect-list`（票 11 派生视图）继续服务宿主 `get_connected_devices`，不重复消费。
3. **连接历史页的设备上下文**：设备列表的「历史」按钮经宿主 router 深链到同插件的
   `session.history` 目录并带 `?deviceId=`；无参数时（侧边栏直入）用设备下拉默认第一台。
4. **设置分组只有两项**：票面 checklist 明列「配对码 TTL、QR 有效期」；端口按上条迁入宿主
   系统分组，会话默认值 / 任务模式默认 / hooks 管理留给后续票（未在本票扩张）。

**5. 测试矩阵（新增/改动）**

| 文件 | 数量 | 覆盖契约 |
| --- | --- | --- |
| 插件 `deviceCenter.test.ts` | 14 | 加载（网络 + 设备）/ 生成码与倒计时 / 无码失败提示 / 取消 / QR 生成与载荷 / 已有 QR 恢复 / 在线离线事件迁移 / 接入清除配对码 / 撤销成功与失败 / 历史跳转 / 移动端请求配对 / 倒计时归零自动清除 |
| 插件 `connectionHistory.test.ts` | 4 | 路由 deviceId 取数分组统计 / 无参默认设备 / 清空成功 / 取数失败不静默 |
| 插件 `pairingSettingsSection.test.ts` | 4 | 读取回显 / 分项保存 / 越界收敛 / 保存失败 |
| 插件 `plugin-contract.test.ts` | 6 | 身份与能力面（8 权限 + 21 api + trait 声明一致）/ 三视图 id 与 manifest 一致 / 槽位值等于宿主内置 order（100 / 200）/ 设置分组槽位 200 且宿主内置分组已摘除 / 前端取数红线 |
| 宿主 `DevicesFallbackView.test.ts` | 4 | 兜底说明 + 设备渲染 / 空态 / 撤销命令与列表重载 / 撤销失败记录且不留假状态 |
| 宿主 `pairing-flow.test.ts` | 5 | 命令门面集成（生成 / 取消 / 恢复 / 列表 / 撤销） |
| 宿主 `useSettingsSections` / `SettingsView` | 改动 | 内置分组 7 → 6（pairing 退役），贡献分组插入位置随之内移 |

**6. 门禁取证（全部实跑）**

- 桌面 `cargo test --no-fail-fast`：lib **1061 passed / 0 failed**、8 个集成目标全 ok
  （`broadcast_shutdown` / `build_manifest_smoke` / `http_auth_biometric` / `link_crypto_http` /
  `pty_session_chain` / `server_integration` / `ws_auth_rules` / `ws_session_route`）、
  doc-tests 1 passed / 2 ignored。含本票新增：`host_impl::platform` 3（IPv4 过滤 / 空输入 /
  真实枚举不回环）、`host_impl::config` 1（两项有效期缺省 / 落库 / 非法值回退）、
  `wasm_runtime::test_session_plugin_artifact_lifecycle`（产物 manifest 8 权限 + 3 视图下的 api 面）。
- 插件 crate `cargo test`：**153 passed / 0 failed**（含 `device_face` 5：活跃记录过滤 /
  倒序 / 宽容解析 / native 显性报错 / 设置键映射）。
- SDK `cargo test`：**85 passed / 0 failed**（含 `ConfigKey` 两键的字面量与白名单穷尽断言）。
- 桌面前端 `vitest run`：**259 files / 759 tests 全绿**。
- 根 `eslint .`：**0 error / 127 warning**（与票 13 同数，零新增）。
- 产物重建：`plugins/session/scripts/build.js`（前端 113.95 kB 单文件 + wasm 976 kB），
  `resources/plugins/desktop/com.bedcode.session/` 三件套 mtime 05:36，`plugin.json` 8 权限 / 3 views。
- 测试后进程清理：cargo / vitest / eslint 均无残留进程（`ps` 复核）。
- **线协议回归零改断言**：`pty_session_chain` / `ws_session_route` / `ws_auth_rules` /
  `http_auth_biometric` / `server_integration` 全绿未改一行（spec D1 自守边界成立）。

**7. `frontend-styles` 自查**

本票 UI 全部为宿主原页的**逐类搬运**（模板、Tailwind 类、token、过渡名、图标 path 完全照抄）：

- 设备页 / 连接历史页的工具栏、Tab、卡片、徽标、空态、分组标题结构与宿主原页一致
  （`wb-toolbar` / `wb-btn-*` / `wb-section-title` / `wb-mono` + `var(--*)` token）；
- 弹窗复用 `PluginModal.vue`（宿主 `Modal.vue` 的逐类复制，尺寸档位与过渡一致）；
- 唯一的样式新增是 `ConnectionHistoryView` 的 `content-visibility` 长列表优化（与宿主原页同款，
  非视觉变化）；无 hex 硬编码、无 `dark:` 覆盖、无新 token、无原生控件外观；
- 网络信息条的 IP 下拉改用 SDK 单源 `Select`（宿主同款组件，`import .../ui`）。

**8. 遗留（归后续票）**

1. **设置分组的完整字段面**：本票只落「配对码 TTL / QR 有效期」两项（票面 checklist 边界）。
   spec D6 提到的「会话默认值 / 任务模式默认 / hooks 管理与全局 hook 清理」仍在宿主设置页
   （`settings.session.*`）或待随任务域（票 15-17）迁入——**归票 17/18 复核**。
2. **端口设置位置变化**：`默认端口` 从「配对设置」分组迁入「系统设置」分组（本票唯一的
   用户可见位置变化）。若需回到插件分组，需要「插件写宿主引擎配置」的原语，属 ADR 0022
   裁剪线议题，另行裁决。
3. **`PluginPageToolbar target="devices"/"history"` 不再渲染**（宿主原页让位后挂载点消失），
   与票 13 会话页同一遗留，归票 18 与页面工具栏扩展点一并复评。
4. **dev-shell 无本插件命令 mock 模块**（票 13 遗留延续）：浏览器 dev-shell 下设备页取数
   走不到 WASM，命令面演示数据（`devMock.pairing`）已备好，接线归后续。
5. **`ui:sidebar` 漏声明的影响面**：本票修正后，票 13 会话页在真实环境下才真正可用；
   建议票 18 的等价回归把「真实宿主启动 + 插件激活 + 侧边栏出现」列入验收（此前只有
   产物级核验，没有真机启动核验）。

**9. 与并行线的交叉复核**

- 本票在 lib.rs 的补丁（`mod device_face;` + 13 条命令面分支）与 pi 的 `pub mod task;` 并存；
  收尾复核 `grep` 命中 6 个新命令字面量、`device_face.rs` 在位、`plugin.json` 8 权限 / 3 views /
  21 api 未被覆盖。
- 插件 crate 的 `declares_only_landed_domain_surface`（权限 + api 面 pin）在票 15 落 api 面时会
  二次变动，两票各自同步该 pin（本票只动 permissions 侧）。
