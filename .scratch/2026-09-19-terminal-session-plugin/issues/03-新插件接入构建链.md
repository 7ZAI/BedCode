# 03: 新插件工程接入构建链与测试面（prefactor）

**父规格:** `.scratch/2026-09-19-terminal-session-plugin/spec.md` D1 / D2 / D8-P0

**What to build:** `com.bedcode.session` 作为空骨架插件可被构建、打进内置资源、被宿主加载并激活成功；插件工程的测试面（Rust crate 单测 + 前端 vitest）从第一天起就在门禁里，不再出现「有插件无测试 include」的缺口。

**Blocked by:** None (can start immediately)

**Status:** code-complete（票内范围全绿：插件产物生命周期闭环 + 插件 crate 3 单测 + 桌面 lib 1010 + 前端 74 files/718 + eslint 0 error）；**唯一未达项**：全量 `cargo test` 的集成目标 `broadcast_shutdown` 红 1 条——**非本票 diff**，证据与归属见 Comments「门禁取证」。

- [x] 插件工程 manifest 就位：id `com.bedcode.session`、`pluginType: rust-ts`、`sandbox: inline`、Application kind、api 与 permissions 空声明起步
- [x] 后端走 wasm32-wasip3 共享构建配置（复用既有 `WASM_TARGET` / cargo env 配置，不自建编译脚本分支），cdylib 直出 Component
- [x] 构建脚本插件白名单、内置资源产物目录、dev watch 收编该插件
- [x] 宿主闭环测试新增「产物可加载可激活」用例（对齐既有插件产物生命周期用例形态）
- [x] 桌面端 vitest include 覆盖新插件 `src/`，并补上 auto-task 目前缺失的 include（为后续搬迁预留）
- [x] 顺带修文档漂移：侧边栏排序值注释与实际内置菜单常量不一致（间隔 100、设备 100 / 会话 200 / 服务器 300 保留 / 插件 9998 / 设置 9999），改正后引用处不再照错注释选槽
- [x] 桌面 `cargo test` + `pnpm run test:run` + `pnpm exec eslint .` 全绿 ← 登记时为存量红灯挡 1 条；票 04 收尾时该红已处置，全量复跑已绿（见下方补记）

## Comments

（实施记录追加此处）

### 2026-09-19 实施落地

**新增插件工程 `bedcode-desktop/plugins/session/`**

- `plugin.json`：id `com.bedcode.session`、`pluginType: rust-ts`、`sandbox: inline`、`main: index.js`、`rustLibrary: bedcode_plugin_session`、`permissions: []`、`api: []`、`contributes.commands: [session.status]`；`kind` 不写即默认 Application（与四个同级插件一致，可停可删且带 UI 贡献，System 形态否决）。
- `rust/`：`Cargo.toml`（`crate-type = ["cdylib"]`、features `native`/`wasm`、SDK path 依赖）+ `src/lib.rs` 骨架（`WasmPlugin` impl + `wasm_entry!`）。骨架**不注册任何扩展点、不申请任何权限**——权限与 api 随语义逐票追加，预声明即审计噪音（D2）。
- `src/index.ts`：前端入口骨架，doc 注释钉住 D2 取数红线（只经 `PluginContext` 与 `plugin_invoke` 通道，禁直调宿主领域命令）。骨架无组件，故未建 `vite-env.d.ts`（P3 搬入视图时随组件建）。
- `vite.config.ts`：`vue() + bedcodePlugin()`（共享模块外部化到 `window.__BEDCODE_SHARED__`），库模式直出 `index.js`；注释里钉住「库模式 CSS 不注入 JS，票 13 搬组件时必须补 `inlinePluginCss()`」的坑（先例见 auto-task）。
- `scripts/build.js`：`--watch / --frontend-only / --rust-only` 三态，target 与 toolchain 一律取仓库共享配置 `scripts/plugin-wasm-config.mjs`（脚本正文无 `wasm32-*` / `RUSTUP_TOOLCHAIN` 字面量，护栏见测试 C2）。
- `package.json`：`build` / `build:frontend` / `dev` / `test:rust`。**与同级插件的一处偏离**：`build:rust` 改为委托 `node scripts/build.js --rust-only`，而非四个同级插件里的裸 `cargo build --target wasm32-wasip3`——后者在默认 stable 下无 wasip3 std，照抄即是个必炸的陷阱脚本（pinned nightly 由 `wasip3CargoEnv()` 注入）。未顺手改同级插件（越界），若要统一另立票。

**构建链接线（四处）**

- `bedcode-desktop/scripts/plugin-build.js` + `plugin-dev.js`：`PLUGINS` 白名单加 `'com.bedcode.session' → plugins/session`。
- `bedcode-desktop/package.json`：`plugins:build` 链追加 `--plugin com.bedcode.session`（CI release 的桌面插件构建入口即此脚本）。
- `scripts/plugin-package-list.json`：`desktop` 列表加 `session`（zip 打包路径）。
- `pnpm install`：新工作区成员入 `pnpm-lock.yaml`（+`plugins/session` importer；同批带入 `plugins/devices: {}` —— 后者是对侧未入库的插件工程被 workspace 扫到，属包管理器正常输出，未手改）。

**实测构建**：`node scripts/plugin-build.js --plugin com.bedcode.session` → vite 产物 `dist/index.js`(79B) + cargo wasip3 release 34.3s + 产物落 `src-tauri/resources/plugins/desktop/com.bedcode.session/`（`bedcode_plugin_session.wasm` 524,808B / `index.js` / `plugin.json`）。`generateManifest` 未改写 plugin.json（声明与源码一致）。

**测试面**

- 宿主闭环（S1）：`src-tauri/src/plugin/manager/wasm_runtime.rs::test_session_plugin_artifact_lifecycle`，与 devices / ai-chatbox 产物用例同构（产物缺失即跳过）——真实 wasip3 组件实例化通过即证明**全部 import 与宿主 linker 匹配**；断言 activate 返回 0、manifest 的 id/pluginType/permissions/api 透传、`session.status` 可调用且回传声明、未知命令返回 `{"error":"Unknown command…"}` 形状、deactivate 返回 0。
- 插件 crate native 单测 3 条：身份五处一致、骨架零声明、命令面往返 + 未知命令报错。
- 前端契约测试 10 条：`plugins/session/src/__tests__/plugin-contract.test.ts`（C1 身份一致 / C2 构建链收口 / C3 入口契约 / C4 D2 前端收口静态红线——扫 src 禁 `@/` 别名、禁 `invoke(`、禁自建 `createPinia()`/`new I18n()`/手写 `__BEDCODE_SHARED__`）。C4 是给票 13/14 预备的评审门禁：出现直调宿主领域命令即红。

**文档漂移修正（本票指名的那处）**：`SidebarPanelDescriptor.order` 注释原写「终端会话 100 / 服务器 200 / 设备 300 / 插件 400 / 设置 700」，与内置常量（`BUILTIN_MENU_ORDERS`：设备 100 / 会话 200 / 服务器 300 保留 / 插件 9998 / 设置 9999）不符——宿主与 SDK 两份镜像均已改正，并补上票 02 落地的契约：**与内置项同 order 值即视为接管该域（内置入口让位），插在两槽之间不触发让位**。`registry.ts` 里 `DEFAULT_VIEW_ORDER` 的同一处错注释随票 01 已修。

**门禁取证**

- `cargo test`（桌面，`--no-fail-fast` 全目标）：lib **1010 passed / 0 failed**（含本票新增用例）、Doc-tests 1 passed / 2 ignored；集成 8 目标中 **7 绿**（`build_manifest_smoke` / `http_auth_biometric` / `link_crypto_http` / `pty_session_chain` / `server_integration` / `ws_auth_rules` / `ws_session_route`），仅 `broadcast_shutdown::broadcast_and_shutdown_flow` 红。
  - 红因：断言「全程无 error 级日志」计到 1 条 —— `bedcode_lib::utils::auth::jwt: jwt: secret store unavailable, falling back to process-random key`（`src/utils/auth/jwt.rs:43`，`host_secrets::get_or_generate` 在无 AppHandle 的 headless 上下文返回 `host secret store not initialized`）。
  - 归属证明：`git diff HEAD -- src/utils/auth/jwt.rs src/utils/auth/host_secrets.rs tests/broadcast_shutdown.rs` **输出为空**（三处均为已提交代码，来自 96d9aef1b 认证中心 v15 线）；且该测试用**空临时插件目录**（`bedcode-itest-plugins-<pid>`，broadcast_shutdown.rs:123），不加载 `src-tauri/resources/` 下任何内置产物 → 本票新增产物不可能进入该路径。本票 Rust 侧只新增一个测试函数与一个独立 crate。
  - 处置：**dev 分支上的存量红灯**（随 96d9aef1b 入库，此前只跑 `cargo test --lib` 故未暴露），不在本票越界改认证线文件——该 ERROR 应降级为 warn、还是 headless 测试上下文注入 secret store，属票 04/05 认证语义下沉的裁决面；本票登记为阻塞项并在此留复跑清单：`cargo test --test broadcast_shutdown`。
- `pnpm run test:run`：**74 files / 718 tests 全绿**（较票 01/02 收尾时 +1 file / +10 tests，即本票新增的插件契约测试经宿主 vitest include 真实进门禁）。
- 根 `pnpm exec eslint .`：**0 error**（123 warning 全为存量，主要在移动端）。
- 格式：未跑 `cargo fmt`——`src-tauri` 下多份在途/存量文件（`wasm_runtime/component.rs`、`host_impl/{auth,peer,database,ws}.rs` 等）按当前 rustfmt 判定均非 clean，全仓格式化会重排对侧在途文件（AGENTS.md §11 文件回滚规范 + 并发线约定）。新增代码按邻近风格手写对齐。

**遗留 / 下一步**

- 票 04 起在 `plugins/session/rust/src/` 下按域建 `pairing/` 等模块，并把 manifest 的 `permissions` / `api` 与实际注册面同步（五同步点见 §7）。
- 骨架未做「首启自动激活」接线（`contributes.lifecycle.onStartup: false`）；P3 前需决定该插件是否随宿主自激活（决定贡献目录在冷启动即可见），归票 13 一并裁。
- `plugins/session/rust/Cargo.lock` 已生成，随工程入库（同级插件同样跟踪）。

### 2026-09-19 票 04 收尾补记（本票登记的阻塞项已解）

本票「门禁取证」里那条 `broadcast_shutdown::broadcast_and_shutdown_flow` 存量红，已由票 04 按 §8 级别表处置：`utils/auth/jwt.rs::resolve_secret()` 对「secret-store 未注入」这条设计内的无宿主上下文路径由 `error!` 降为 `warn!`。票 04 收尾时全量 `cargo test --no-fail-fast` 为 **lib 1013 + 8 集成目标 + doc 1 全绿**，本票最后一项据此勾上。归属仍在票 04 的 Comments §6。
