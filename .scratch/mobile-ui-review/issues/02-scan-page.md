# 02 扫码页：取景器遮罩 + 工具栏

**Status:** ready-for-agent
**Type:** task

## 问题（vision 第三批，P0）

`scan-dark.png`：
1. 扫码框外无压暗遮罩（框内外同色，无法锁定扫描区）——标准模式：外部 `rgba(0,0,0,0.6)` 压暗 + 框内透明 + 四角高亮
2. 缺工具栏：手电筒 / 相册 / 我的二维码
3. 扫码框位置偏上，框与提示文案间 ~150px 留白
4. 缺相机权限错误态/降级提示

## 涉及

`src/views/ScanView.vue`（html5-qrcode 实现）

## 建议

- 取景器遮罩：`absolute inset-0 bg-black/60` + mask 挖洞或四角 L 型定位标 + 内部扫描线动画（2s 循环）
- 底部工具栏三按钮（手电筒/相册/我的二维码），等宽分布
- 相机不可用时显示错误态 + 重试/去设置引导

## 验证

harness 截 `scan` 深色/浅色，确认遮罩层次与工具栏。

## Comments

- 2026-08-12：vision 第三批评审发现（P0）。
- 2026-08-12（已修复）：四块压暗遮罩（rgba(0,0,0,0.6) 挖窗）+ 四角定位标 + 2s 循环扫描线；提示文案移至框下消除留白；底部工具栏（手电筒/相册/我的二维码，44px 触控区）；相机权限错误态加引导文案；相册扫码失败改 toast 不打断相机；stop() 加 isScanning 守卫修复相册路径。
