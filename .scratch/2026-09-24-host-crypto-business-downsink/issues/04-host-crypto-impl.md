# 04: 宿主 host_api/crypto.rs 实现 + 权限门 + 审计

**What to build:** 把票 03 的契约翻译为宿主侧可执行原语：WIT `host-crypto` 每个函数一个宿主管道，按名字从 `crypto/` 注册表取实现执行。每个原语入口先过权限门（对应 `crypto:*` 权限位，未授权即 fail-visible 拒绝、绝不在池线程触发弹窗），算法名再过白名单，成功后落审计日志（含插件 id、原语、算法名）。插件经此按名调用宿主加密做加解密往返。安全约束：原语只给中性算法操作、不给业务编排；宿主密钥（身份/keystore）不暴露给插件，只处理外部传入的密钥材料。

**Blocked by:** 03

**Status:** done（2026-09-24 落地；host_api/crypto.rs + 7 单测）

- [x] `host_api/crypto.rs` 实现全部原语，按名从注册表调度，按名从注册表调度；返回统一错误类型带操作上下文
- [x] 权限门：未申请（fail-visible）；三域隔离单测通过 `crypto:*` 的插件调用被拒（fail-visible）；三域拆分与 manifest 声明一一对应
- [x] 算法名白名单：注册表外拒绝 + 成功审计（只记算法名不落密钥）；审计日志含 `[plugin:xxx] host-crypto:* name=…`（算法名，不含任何密钥/明文）
- [x] host_api 单测：往返/拒绝/域名隔离/长度校验 7 项；端到端 wasm 通路经四插件重建验证成功；无权限位被拒；白名单外被拒；宿主密钥不出原语（grep 断言）