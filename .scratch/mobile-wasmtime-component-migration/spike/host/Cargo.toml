[package]
name = "spike-host"
version = "0.1.0"
edition = "2024"

[dependencies]
# 版本锚定（ADR 0019）：wasmtime 47 两端锁死
wasmtime = "=47.0.3"
# 组件编码（与桌面端 tools/componentize 同生态）：
# 0.256 为当前最新，若与 0.60 产物不兼容会在 spike 中暴露
wit-component = "=0.256.0"
anyhow = "1"
serde_json = "1"