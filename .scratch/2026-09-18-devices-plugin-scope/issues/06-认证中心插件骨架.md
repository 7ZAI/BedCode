# 06: 认证中心插件骨架（B1）

**What to build:** 新建认证中心插件工程（`plugins/devices/`，headless rust-only 后端，无 UI；设置面经宿主命令桥接读取状态，见票 11）：**wasm32-wasip3** target + async 编排；manifest 声明 api + permissions（auth / storage / database）；activate / deactivate / command 导出；在 async 宿主中加载激活闭环。依赖：hmac / sha2 / ed25519-dalek / getrandom（wasip3 target 可用，SDK 核心不膨胀）。

**Blocked by:** 03, 04（wasip3 构建链 + host-auth 原语可用）

**Status:** done（2026-09-19）

- [x] 插件在 async 宿主中加载、激活、可调用（生命周期闭环，06 框架系统组件装配）
- [x] manifest 声明 api 与 permissions；未声明权限快速失败（前端）+ Rust 端最终仲裁
- [x] 插件 cargo test 全绿；产物为 wasip3 Component
- [x] headless：无 UI 组件；mock/种子数据不落 dev-shell（归插件入口导出 devMock）

## 验证证据（2026-09-19）

- 宿主新增 `test_devices_plugin_artifact_lifecycle`：加载真实产物（resources）→ 激活 → 命令调用 → 停用，全绿；manifest api/permissions 透传断言 + 未知命令快速失败
- 全量 `cargo test --lib` **955 passed / 0 failed**（连续两次稳定；首次全量跑 5 失败为已知 fixture 并发重建竞态，复跑即过）
- 产物 magic 验证：`0061736d`（\0asm）+ `0d 00 01 00`（Component）= wasip3 cdylib 直出 Component，免 componentize
- 前端权限快速失败：permission.ts VALID_PERMISSIONS 含 auth（票 04）+ storage ✓；Rust 端 SDK permission.rs VALID_PERMISSIONS 同集 ✓

**产物**：

- `plugins/devices/`：`plugin.json`（pluginType rust-only、rustLibrary bedcode_plugin_devices、permissions [auth, storage]、api [com.bedcode.devices.hello]）+ `rust/`（Cargo.toml + lib.rs）+ `package.json` + `scripts/build.js`（wasip3 构建 + 复制 resources，无前端）
- lib.rs：`#[plugin_api(manifest="../plugin.json")] trait DevicesApi { fn hello() }`（ADR 0017 编译期防漂移）+ `WasmPlugin`（activate 注册 dispatcher / deactivate / on_message 分派 / `devices.status` 命令）+ `wasm_entry!`
- resources/plugins/desktop/com.bedcode.devices/：wasm 584KB + plugin.json

**说明**：

- 「database」非独立权限——主库/存储原语均由 `storage` 权限门覆盖（host_impl/database.rs + storage.rs），故 manifest 只声明 auth + storage；文档/注释已写清
- `#[plugin_api]` 宏默认读 `<CARGO_MANIFEST_DIR>/plugin.json`（rust/ 下），须显式 `manifest = "../plugin.json"` 指向仓库根 manifest；宏生成代码需 `HostBus` trait 在作用域（bus_subscribe）
- 密码学依赖（hmac/sha2/ed25519-dalek/getrandom）留票 07/08 随业务模块引入，骨架期零膨胀
- headless：无 vite.config/tsconfig/dist/index.js，前端 loader.ts 对 rust-only 直接跳过（不加载 main）
- mock/种子数据：骨架期无业务 mock；后续模块的 mock 归插件入口 devMock 导出（未落 dev-shell）

**后续**：票 07 pairing（HS256 RFC7515 向量，对照宿主实现行为等价）→ 08 trust → 09 consent + 互调 api 扩充（同步 manifest.api 与 trait，宏自动防漂移）。
