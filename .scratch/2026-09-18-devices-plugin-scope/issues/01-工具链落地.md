# 01: wasip3 工具链落地

**What to build:** 桌面插件 wasm32-wasip3 构建工具链进仓库并可复现：固定 nightly 版本提供 wasm32-wasip3 target（stable 1.98.1 无预编译产物，spike 已在 nightly 1.100.0 验证），安装脚本含镜像加速（RUSTUP_DIST_SERVER），工具链选择与切换点（stable 1.99 发布后）写入文档，构建/测试命令字眼随工具链同步。

**Blocked by:** None（can start immediately）

**Status:** done（2026-09-19，验证证据见下；切换点 stable 1.99 为后续监控项）

## 交付物

- `scripts/wasip3-toolchain.sh` —— 单一事实来源（`WASIP3_NIGHTLY=nightly-2026-09-16`，
  默认 USTC 镜像可覆盖）；子命令 `install`（幂等）/ `verify` / `fixture` / `health`
- `bedcode-desktop/packages/plugin-wasip3-test/` —— wasip3 fixture 工程
  （`com.bedcode.wasip3-test`，`wasip3-test.read-clock` 命令走 wasi:clocks import）
- `docs/knowledge/wasip3-toolchain.md` —— 决策 / 安装 / 构建 / 迁移点文档

## 验证证据（2026-09-19 实测）

- [x] `wasm32-wasip3` target 在固定版本（nightly pin，构建脚本 RUSTUP_TOOLCHAIN）下安装成功，全流程脚本化可复现
  - `scripts/wasip3-toolchain.sh install`（RUSTUP_DIST_SERVER=USTC 镜像）→
    `rustc 1.100.0-nightly (215a8af4b 2026-09-15)` + `wasm32-wasip3` target 就绪；
    `verify` 幂等通过
- [x] 至少一个最小 fixture 以 wasip3 target 编译通过，产物为 Component（magic 校验：`\0asm` + `0d 00 01 00`，cdylib 直出免 componentize）
  - `scripts/wasip3-toolchain.sh fixture` → `bedcode_plugin_wasip3_test.wasm`（300K），
    `od` 前 8 字节 `00 61 73 6d 0d 00 01 00` ✅
  - wasmtime 48.0.2 解析（复用 spike validator）：import = `bedcode:plugin/host-log`
    + 全套 `wasi:cli@0.3.0` / `wasi:clocks@0.3.0`；export 8 接口与 unknown-unknown 同构
- [x] 工具链决策（nightly 版本、镜像源、stable 1.99 迁移点）记录进文档，命令字眼与 AGENTS.md §3 一致（无旧 npm 字眼）
  - `docs/knowledge/wasip3-toolchain.md`；命令一律 `cargo test` / `pnpm run test:run` /
    `pnpm exec eslint .`
- [x] 存量插件在 wasip3 target 下零代码改动可编译（spike 已验证，作为工具链健康基线保留）
  - `scripts/wasip3-toolchain.sh health`：4/4 通过，零代码改动 ——
    file-transfer 860K / ai-chatbox 716K / agent-hub 1.2M / auto-task 1004K，均 Component

## 边界（转票 02/03）

- wasip3 产物 import wasi 0.3，实例化需宿主 **p3 async linker（票 02）**；
  p2 sync 宿主不可加载 —— 产物仅编译链验证，不得替换 `resources/plugins/` 现行产物
- CI（dtolnay stable + unknown-unknown/wasip2 targets）切换随票 03 或 stable 1.99