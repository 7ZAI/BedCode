# 01: wasip3 工具链落地

**What to build:** 桌面插件 wasm32-wasip3 构建工具链进仓库并可复现：固定 nightly 版本提供 wasm32-wasip3 target（stable 1.98.1 无预编译产物，spike 已在 nightly 1.100.0 验证），安装脚本含镜像加速（RUSTUP_DIST_SERVER），工具链选择与切换点（stable 1.99 发布后）写入文档，构建/测试命令字眼随工具链同步。

**Blocked by:** None（can start immediately）

**Status:** ready-for-agent

- [ ] `wasm32-wasip3` target 在固定版本（nightly pin，仓库级 rust-toolchain 或构建脚本 RUSTUP_TOOLCHAIN）下安装成功，全流程脚本化可复现
- [ ] 至少一个最小 fixture 以 wasip3 target 编译通过，产物为 Component（magic 校验：`\0asm` + `0d 00 01 00`，cdylib 直出免 componentize）
- [ ] 工具链决策（nightly 版本、镜像源、stable 1.99 迁移点）记录进文档，命令字眼与 AGENTS.md §3 一致（无旧 npm 字眼）
- [ ] 存量插件在 wasip3 target 下零代码改动可编译（spike 已验证，作为工具链健康基线保留）
