# 交接：桌面 WASM 内核审计修复（进行中）

Date: 2026-09-22（本版写于票 03 与票 06 完成并提交之后；上一版内容见 git `efc4cafc7`）
读者: 接手本任务的新会话（假定零上下文）
任务根: `.scratch/2026-09-21-wasm-core-audit/`
结论与证据真源: `spec.md`（审计结论）+ `microkernel-gap.md`（微内核差距）
工作清单真源: `issues/01..13`（每张票自带验收表 + 实施记录 + 裁决块）

---

## 1. 30 秒版

- 本轮审计结论：沙箱边界成立，**安全授权不成立**。修复拆成 12 张票 + 测试清理票（13）+ 新立票（14）。
- 已完成并提交：**票 01（权限词汇单源）→ 02（P0-1 主库 SQL 隔离）→ 04（P0-3 会话/终端属主）
  → 05（P0-4 总线 topic 命名空间 + P1-4 互调回复道身份门）→ 03（P0-2 桌面审批链 + 内容钉扎接线）
  → 06（P0-5 前端通道身份由宿主凭证绑定 + `sandbox` 字段退役）**。
- ✅ **P0 全部完成**（01/02/03/04/05/06）。✅ **票 11（内核去重与归位）2026-09-22 完成并提交**
  `709d4c252` + `5d8bf9e93`：实例化（`instantiate_wasm_plugin`）与 contributes 注册
  （`register_plugin_contributions`）各收一条入口并配两条源码锁 + 行为锁（均做过变异自检）、
  删 `granted_permissions` 死字段、`downloader` 归位 `manager/`、manifest 必填校验收一处真源、
  code-map/AGENTS 口径对齐（含 fs_auth 四层）。门禁：lib 1138 passed / 0 failed，全 target 绿，`[skip]` 0。
- 剩下的是 P1 结构票：`07`（fs_auth 硬化）、`08`（插件 HTTP 认证，裁决已收齐）、
  `09`（任务超时与生命周期）、`10`（monitor 与配置面）、`12`（内核业务清零，裁决已收齐：
  peer-net 选项 A）——均 ready-for-agent。
- **测试面两张可插队**：`13`（五个集成 target 断链，做完 `cargo test` 全 target 才恢复成真门禁）、
  `14`（`wasm_hash` 生产者——票 03 的登记项，打包链注入，**开工前需先定三选一注入口径**）。
- 同 worktree 另有一条活跃线（PTY 业务语义下沉 / terminal-into-session），其 spec 在
  `.scratch/2026-09-22-pty-business-downsink/`；它正在改 SDK `permission.rs`（退役 `session:config`）、
  WIT、`session/`、`host_impl/session.rs` 与两个集成 target —— 撞车区见 §7。

## 2. 提交状态与编号陷阱（分支 `dev`）

**三条线共用一个 worktree，且都用了「票 0N」编号。** 本票面的票号一律指
`.scratch/2026-09-21-wasm-core-audit/issues/`；读 log 时见到「票 02/03/04/05/07」大概率是**对侧线**的：

| commit | 线 | 内容 |
| --- | --- | --- |
| `1a81fcca8` / `cca8da48c` / `7f3597931` / `af4e1a787` | 本线 | 立项 + 票 01 / 02 / 04 |
| `87a42b50b` | 本线 | 票 03/08/12 裁决收齐 + 上一版交接 |
| `f9849fd0b` / `5d6b50c99` / `d0bd98d8a` / `cc74d76d9` | **对侧**（terminal-into-session） | 其票 02/03a/04/05 |
| `36a142027` / `c284f92ae` | **对侧** | 其票 07（私有库 id 路径迁移）/ 票 08（文档同步） |
| `e94b0db2a` / `d3eecff73` / `da66203cc` | 本线 | 票 05：票面裁决 → 实现 → 残余旧串清理 |
| `68f6f5812` / `33b085905` / `400b4c79f` | 本线 | 票 03：宿主 Rust + SDK manifest 字段 → 前端弹层与文案 → ADR/CHANGELOG/code-map |
| `b17316545` | 本线 | 票 03 票面取证（scratch 文档被对侧提交带走） |
| `7825359bd` / `0b7c2c02a` / `f7354f562` | 本线 | 票 06：宿主凭证与 sandbox 退役 → 前端带凭证 → ADR/CHANGELOG/code-map |

**已发生的历史污染（用户 2026-09-22 表态「散落没有关系」，不再重整）**：对侧
`5b008eb5c` 把本票 05 当时在途的 5 个宿主文件一起提交了，`c284f92ae` 又带走了本票写在
AGENTS.md / CHANGELOG.md / code-map.md 的文档行 ⇒ 票 05 散在 `5b008eb5c` / `c284f92ae` /
`d3eecff73` / `da66203cc` 四个 commit 里。教训与对策写进票 05 实施记录第 9 条
（**本线小步勤提交；共享文档尽早落成自己的 commit；提交前逐文件核 `git diff --numstat`**）。
对侧改名清扫还把本票新写的 `api_registry.rs` 测试串 `com.bedcode.session.*` 换成了
`com.bedcode.terminal-session.*`（测试内自洽字面量，无行为影响）。

## 3. 已完成五票：只需知道的结论

- **票 01**：权限词汇唯一真源 = 桌面 SDK `permission.rs` 的 `PERMISSION_VOCABULARY` 反射表，
  两份生成物（CLI json / 前端 ts）由 `pnpm run gen:permissions` 重出；宿主 `plugin/permission.rs`
  五条锁 + 前端五组锁，**加/拆权限位漏跑生成器即转红**。
  后置项（展示文案只覆盖 13/31）是**票 03 的前置子项**。
- **票 02**：主库 SQL 边界从正则挪进 SQLite authorizer（`with_main_db_guards`），
  `storage` 不再自动授予，新增高危位 `database:main`；`validate_sql_table_prefix` 只是早失败文案。
  登记了一条**未修的既有缺陷**：`reject_bare_transaction_control` 会把
  `CREATE TRIGGER … BEGIN … END` 误判成裸事务控制 → 主库面触发器不可用（见票 02「过程中发现」）。
- **票 04**：内核加不透明表 `session_owners`（不进 DTO → 线协议零变化），判定链固定
  **权限门 → 参数门 → 属主门**；`session_lifecycle_register` 补 `session:read`；
  注解槽做成「属主独占写入」而非票面字面的命名空间（理由见票 04 实施记录第 5 条）。
  2026-09-22 复核：交接旧版说的「对侧 `output-ring-fetch` 未过属主门」**不成立**——
  实现与三条用例对侧在 `cc74d76d9` 已自带（注释点名票 04），已在票 04 末尾记为「无遗留尾项」。
- **票 05**：总线访问控制由 **topic 形态**决定——`<plugin-id>::<name>` 是收件箱，
  只有属主与宿主可订阅/可发布；判定只看串，不查激活表/安装表（机制 A 会在属主未激活时漏判）。
  宿主定向事件（`pty:exit` / ws 五事件 / `mdns:found|lost`）改投该形态；legacy 旧串
  （`pty:exit.<own-id>`）**订阅面显式拒绝并回带新形态**，不留静默断流。
  互调回复道：订阅面对 WASM 关闭 + `ReplyHandler` 校验 `sender` == api 声明属主
  （SDK correlation id 是可猜的 `req-N`，改前实测伪造回复能被当成调用结果返回）；
  顺带修掉 `api_call` 的回复订阅泄漏。原语单一真源在 SDK `host::bus`，宿主与插件共用。
  移动端不跟演（代码与版本号未动），三处偏离清单在票 05 Comments 与 AGENTS §7。
- **票 03**：审批门禁从「覆盖良好的死码」变成激活路径上的闸门 —— `activation.rs::approval_gate`
  在**预授权之前**裁决（不持 `plugins` 锁，目录哈希走 `spawn_blocking`）：只作用 `UserInstalled`
  （随包内置来源沿用 ADR 0020 的构建信任域），无记录/哈希失配 → `NeedsApproval` + 拒绝激活
  （失配并撤销批准）；**生效集写进 `PermissionManager`**（只算不授 = 门禁可绕过），
  `effective_permissions` 的 `storage` 无条件插入与票 02 取消的默认位对齐。
  `compute_dir_hash` 排除插件私有库 `plugin.db` 及 SQLite 边车（**不排除会让「批准 → 启用 → 再启用」
  自判内容替换并撤销自己的批准**，这是本票最容易漏的一处）；存量兼容走 boot 自动批准一次 + `warn`。
  前端：`PluginApprovalDialog.vue`（列表页/详情页共用，`NeedsApproval` 时「启用」改为先审批后启用）+
  权限文案由 14 补到 **31** 条（zh-CN/en）+ 高危四位红标与后果文案。
  安装期补 `wasm_hash`（可选字段，**目前无生产者**：打包 CLI 不注入，与移动端现状一致）与
  zip 三类上限（512 条目 / 64 MiB / 32 MiB，按实际写入字节裁决）。
  双端偏离（`storage` 特例仍在移动端）记进 ADR 0020 修订记录。

## 3b. 票 06 结论（P0 收尾）

- **身份从参数搬到凭证**（Tauri 命令面无调用者身份，同 webview 内宿主与全部插件前端同权）：
  **loader 会话密钥**（宿主前端面，每次页面加载**首个调用者生效**，宿主前端在导入任何插件模块之前取，
  由 `on_page_load` 钩子重置）+ **插件通道令牌**（插件面，用 loader 密钥换、运行态才签发、停用即回收）。
  裁决 fail-closed：令牌只能操作自己（不符即拒并点名两侧），loader 密钥可操作任意目标（宿主职权），
  凭证无效一律拒绝、不落回按参数放行。
- 顺带关闭两条同族缺陷：**`plugin_invoke` 此前连权限门都没有**（只查目标激活），
  **`plugin_fs_auth_respond` 无身份约束**而授权请求事件是广播的（插件可自答自己的文件访问请求）。
- `sandbox` 退役（裁决 2）：字段承诺 `isolated` 而实现只有「非 inline 跳过」，前端与宿主同权共生、
  不存在隔离 → 从 SDK manifest、四个 plugin.json、loader 分支与两份 TS 类型全删；旧产物带该键照常加载
  且不再回写。**SDK 公开字段退役，下次发版需按 0.x 记 breaking**。
- 诚实边界（不要当已解决）：这是「提高冒充门槛」不是隔离；宿主管理面命令（activate/install/uninstall/
  approve/dev-reload）按裁决不绑定；插件前端的 Tauri 事件订阅面无法按调用者绑定；移动端同族问题未修
  （本票桌面-only）。真机手工回归（GUI 起四插件）未执行，只有测例面证据。

## 4. 下一票开工须知

1. **先看 `git status` + `git log --oneline -10`**：对侧线活跃，`utils/auth/`、`session/`、
   `server/`、`plugins/terminal-session/`、`plugin_controller.rs` 是撞车高发区。
   重叠时按用户既定做法：**先做不重叠的独立文件，共享接线单独征求确认**；
   不碰对侧在途文件、不抢构建锁、证据记进票面。
2. **票 03 已完成**（`68f6f5812` / `33b085905` / `400b4c79f`）。留下两条登记项，别当待办顺手做：
   `wasm_hash` 无生产者（要自动注入得动打包链，单独评估）；「权限清单变更时重弹」按裁决不做。
   注意**对侧在途把 `session:config` 从词汇表退役**（其 spec 阶段 1/2），本票文案表为兼容两态保留了该条，
   退役落地后可删（文案表不是词汇漂移锁的一部分，不会因此转红）。
3. **票 06 已完成**（`7825359bd` / `0b7c2c02a` / `f7354f562`）。它留下的两条边界别当待办顺手做：
   宿主管理面命令不绑定（裁决 3 范围外）、事件订阅面无法绑定（Tauri 事件无 per-监听者归属）——
   都写在票 06 实施记录第 8 条。真机手工回归（GUI 起四插件验证 storage/invoke/事件订阅）也列在那儿。
4. **下一张票建议顺序**（均为 ready-for-agent）：`12`（内核业务清零，裁决已收齐）→
   `08`（插件 HTTP 认证）→ `07` / `09` / `10`（结构面）；`14`（`wasm_hash` 生产者）
   **开工前需先定三选一注入口径**（源/产物/占位符），且会与「产物/源 manifest 逐字一致」
   的既有惯例打架，见票面。
   **票 11（结构面）/ 13（集成 target 断链）/ 15（构建链权限映射漂移）均已 done**。
5. **票 05 遗留的一条设计约束**：`MessageBus::subscribe_static` 仍是宿主侧**无门禁**入口
   （`peer_net` 刷新订阅、api 回复订阅靠它）。若将来出现插件可触达的静态订阅路径，必须另设门禁。
6. **改权限词汇记得重跑生成器**（`pnpm run gen:permissions`）、**改 WIT 记得看 AGENTS §7
   的双端偏离段**（当前 desktop v22 / mobile 11；`host-bus` topic 命名空间是「不 bump 的行为变更」，
   旧产物需按 v22 SDK 重建）。

## 5. 门禁跑法（本机实测有效形态）

```bash
# 宿主 Rust（票面级验证用 --lib 足够；全 target 见 §6）
cd bedcode-desktop/src-tauri && cargo test --lib

# 防假绿（改/删函数面后必跑）
cd bedcode-desktop/src-tauri && cargo check --lib --tests

# 前端（默认 pnpm run test:run 在本机会 OOM，必须换 forks 池）
cd bedcode-desktop && NODE_OPTIONS=--max-old-space-size=4096 \
  pnpm exec vitest run --pool=forks --maxWorkers=2
pnpm exec eslint .          # 根目录，0 error 门禁

# SDK
cd bedcode-desktop/packages/plugin-sdk-desktop/rust && cargo test --lib
```

- **宿主 `cargo test` 前重出插件产物**（否则闭环用例 `[skip]` 静默算过）：
  `node bedcode-desktop/plugins/terminal-session/scripts/build.js`
  （**目录与 id 已于对侧票 06 改名：`plugins/session` → `plugins/terminal-session`，
  `com.bedcode.session` → `com.bedcode.terminal-session`**），file-transfer 等同理；
  跑完在日志里核 `[skip]` 计数为 0（`grep -c skip` 命中的多是测试名子串，要看前缀 `[skip]`）。
- **CRLF 纪律**：`host_impl/lifecycle.rs`、`host_impl/api.rs`、`host_impl/bus.rs`、
  SDK `permission.rs` / `host/bus.rs` / `api_call.rs`、`src/locales/{zh-CN,en}/desktop.ts`、
  `plugins/terminal-session/rust/src/task/*`、`src-tauri/tests/*.rs`（**除 `pty_session_chain.rs` 是 LF**）
  等是 CRLF。**禁止整 crate `cargo fmt`**；`rustfmt --check` 通过 **≠ 写回安全**——
  2026-09-22 实测 `host_impl/api.rs` 被 `rustfmt <该文件>` 直接转成 LF，diff 瞬间变 522/336 全文重排，
  只能按 HEAD 的 CR 数手工还原。改 CRLF 文件优先 Edit 工具；脚本必须 `newline=''` + 显式 `\r\n`，
  改完双向核 `git diff --ignore-cr-at-eol` 与 CR 计数。
- 跑完测试检查并清理残留进程 / 监听端口（本线不 spawn mock server，一般无残留）。
- **新增锁必须做变异自检**（票 01/02/04/05 四票沿用：旁路门禁 → 各自转红 → 还原；
  票 05 三次分别转红 6 / 7 / 1 项）。写不出「它变红」的断言就是没承重的断言。

## 6. 仓库既有红（非本线造成，勿顺手修）

`cargo test` 全 target 在 **HEAD 就编译不过 5 个集成 target**（2026-09-22 逐 target 复核确认，
不是 4 个；`cargo check --lib --tests` 的汇总行会因缓存只列出部分，
判数要 `cargo check --test <name>` 逐个跑）：
`ws_session_route` / `pty_session_chain` / `ws_auth_rules` / `http_auth_biometric` / `broadcast_shutdown`，
报的全是已退役符号（`server::services::pairing_service`、`AppContextBuilder::pairing_service`、
`utils::auth::QrTokenManager`、`SessionManager::from_database`、`restart_session`）。
`cargo test --lib`（票 03 收尾 1126 passed / 0 failed；票 06 收尾 **1132 passed / 2 flake**，见下）完全看不见它们。
**已单独立票 `issues/13-integration-target-broken-links.md`**（含逐 target 符号表、退役来源、
现役替代路径、行尾纪律、验收）。票 03 收尾实测逐 target 错误数：`ws_session_route` 3 /
`pty_session_chain` 5 / `ws_auth_rules` 5 / `http_auth_biometric` 4 / `broadcast_shutdown` 4
（后两个对侧在途又改了，数字会动）。

另有**前端既有红（非本线）**：`plugins/terminal-session/src/__tests__/plugin-contract.test.ts`
的权限数组 pin 与对侧在途改动脱节（其对侧随后已同步）；`vue-tsc --noEmit` 仅
`src/dev/terminal-mock/TerminalMock.vue` 3 条 `@/utils/terminal*` 缺失（终端下沉批次留下的
dev mock 断链，`git status` 未显示该文件被改 ⇒ 既有）。

另有**既有时序抖动**：`ws_e2e::test_ws_endpoint_server_domain_roundtrip`、
`task_e2e::test_task_submit_events_dispatched_and_status`、
`terminal_output_perf::perf_p2_guest_ring_fetch_batch_curve` 在高并发负载下偶发失败
（5s 轮询真等 / 吞吐曲线对负载敏感；票 06 收尾跑全量时前端 vitest 并发占用机器，两条同时红），
单跑与复跑绿。报数时如实写「N 次里 M 次全绿 + 抖了哪条」，别粉饰成恒定绿。
**不要在前端全量测试并发跑宿主全量测试**——两边的时序/吞吐用例会互相干扰。

## 7. 工作区

**本线干净**：票 03 的改动落在 `68f6f5812` / `33b085905` / `400b4c79f` / `b17316545`，
票 06 的改动落在 `7825359bd` / `0b7c2c02a` / `f7354f562`（外加本文件所在的 scratch 提交）。
`git status` 里的其余条目全是**对侧在途**（PTY 业务下沉线，`?? .scratch/2026-09-22-pty-business-downsink/`），
清单随其进度变化，**一律别 add**。票 06 收尾时的对侧在途面：
`src-tauri/src/pty/{command,pty_handler,pty_process}.rs`、`src-tauri/src/session/session_manager.rs`、
`src-tauri/src/enums/shell.rs`、`src-tauri/src/events/sync_handler.rs`、
`src-tauri/src/plugin/manager/wasm_runtime/host_impl/{session,terminal}.rs`、
`plugin/manager/wasm_runtime/tests/session_e2e.rs`、
`plugins/terminal-session/rust/src/{launch,lib}.rs`、`packages/plugin-sdk-desktop/rust/wit/bedcode.wit`
（其 ABI v23 的配置面写原语退役已在 `fc760448e` 落地）。

**共享文档交接纪律（票 03 实证）**：`code-map.md` / `AGENTS.md` / `CHANGELOG.md` 是跨线共用文件，
首次提交文档时整文件 `git add` 会把对侧在途 hunk 一起提交（本次先误提交为 `cdf2a00cc`，
已 `reset --soft` + blob 精确暂存修正为 `400b4c79f`）。提交这类文件前**逐文件核 `git diff --numstat`**，
必要时用 `git hash-object -w` + `git update-index --cacheinfo` 局部暂存。

另有 `?? bedcode-desktop/resources` → 指向 `src-tauri/resources` 的**符号链接**（2026-09-19 建，
插件产物目录），不是本线产物，**别 add**。

## 8. 用户已确认的工作方式（本项目内）

- **一张票做完先提交再开下一张**；票据/spec 单独 `docs(scratch)` commit，实现各自一个 commit。
- commit message 用中文 conventional commits，**必须写实际跑过的门禁数字**（用户拒绝照抄票里的历史证据）。
- **禁止 commit message 出现 AI 协作者标记**（AGENTS §11）。
- 遇同 worktree 对侧在途改动：**不碰对侧文件、不抢构建锁、记证据等收尾**，不擅自 stash/checkout 对侧内容；
  **历史被对侧卷走时不改写对侧 commit**，取证写进票面并报告用户。
- 拆票偏好纵向 tracer-bullet + 前置 prefactor；资源上限类取舍优先「调用方声明 + 宿主上下限 + 越界 fail-visible」。
- 下沉按产品闭环并插件、**界面维持**、能力只经 `host-*` 原语；可单端先做完，另一端破损**列清单显式登记**
  （移动端代码与版本号一律不动）。
- 不确定的设计取舍先问，不猜；安全/架构红线（AGENTS §5/§8）不可静默越过。
