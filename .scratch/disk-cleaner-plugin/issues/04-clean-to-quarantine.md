# 04 — 清理动作 → 隔离区批次

**What to build:** 用户在扫描结果页点「清理」，勾选目标的命中文件全部移入隔离区（`{home}/.bedcode/cleaner-quarantine/<batchId>/`），写入批次元数据（类别、逐文件原始路径、大小、时间），完成后展示释放空间摘要。被占用的文件一律跳过并计数（「N 个文件被占用已跳过」），绝不杀句柄。真实清一次用户 TEMP 目录即可端到端验证。

**Blocked by:** 03

**Status:** ready-for-agent

- [ ] 删除仅以「move 入隔离区」形态发生，插件代码中不存在绕过隔离区的直接 remove 业务路径（spec §7 专项检查项）
- [ ] 批次元数据完整记录原始路径等信息，足以支撑后续还原
- [ ] 占用文件跳过并计数，清理流程不因个别文件失败中断
- [ ] 清理完成展示释放空间摘要
- [ ] 真实 TEMP 目录清理演示通过（move 后原位置为空、隔离区内容完整）
