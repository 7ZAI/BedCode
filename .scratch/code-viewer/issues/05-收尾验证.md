# 05 i18n 核查与端到端收尾

Type: task
Status: ready-for-agent
Blocked by: 04

> 规格依据：`.scratch/code-viewer/spec.md` 验收总纲。本 issue 不做新功能，只做横切收尾。

## 任务

1. **i18n 双端核查**：全量 grep 本 feature 新增文案，确认 zh-CN / en 一一对应、无中文硬编码残留（AGENTS.md 强制「i18n key 同步出现在 zh-CN 和 en」）
2. **端到端烟测**（dev 手动）：展开/收起几何往返、终端视口不变、标签 16 上限、自动重载（在终端里 `echo >> file` 看面板刷新）、Ctrl+F、dotfiles 开关、GBK 文件显示、拒绝展开分支（可用临时小窗宽验证）
3. **代码质量收尾**：
   - `lens_diagnostics mode=all` 无遗留 blocker（pi-lens 纪律）
   - `cargo test` + `pnpm run test:test-run`全绿
   - 评分对照 `spec.md` 验收总纲逐条打勾
4. **文档一致性**：CONTEXT.md 词汇区与实现竞合检查（实现若与术语冲突，改实现或按实际修订词汇表）；ADR 0023/0024 的 Consequences 是否兑现
5. 输出：在 `.scratch/code-viewer/issues/05-*.md` 评论区 `## Comments` 追加烟测结果摘要

## 验收标准

- [ ] 验收总纲逐条勾选通过，无遗留
- [ ] 双端 i18n key 对照无缺漏
- [ ] grate 记录（烟测摘要）落盘

## 相关代码

- 本 feature 全部新增/修改文件；`docs/adr/0023/0024`；`CONTEXT.md`；`.scratch/code-viewer/spec.md`