# 07 — 识别流水线（det/cls/rec + 行排序 + 置信度）

**What to build:** 真正的 OCR 能力：一张 RGBA8 图片 → 文本行列表（按阅读顺序、原图坐标系 bbox、逐行置信度 0~1、空文本行剔除）；首次识别惰性加载引擎（解压 + 建 3 个 ort session）并常驻缓存；识别全程 `spawn_blocking` 后台线程；并发请求单飞串行排队。实现依据为 spec §4.3 + §8 性能目标。用户可感知：`recognize` 返回正确文本行，第二次识别不再等模型加载，识别中再触发排队不崩。

**Blocked by:** 06（需要模型文件 + ort session 才能推理）

**Status:** ready-for-agent

- [ ] PP-OCRv4 三段流水线（det → cls → rec）用 ort session 推理打通
- [ ] 行排序：det 输出自上而下、行内从左到右（阅读顺序）；bbox 换算回原图坐标系（Kotlin 解码尺寸）
- [ ] 空文本行剔除；`confidence` 为 rec 逐行置信度（0~1）
- [ ] `cargo test` 通过：合成 RGBA 图测预处理（灰度/归一化/det 缩放）与排序/剔除/bbox 换算逻辑（模型推理留真机验证）
- [ ] 惰性加载 + 常驻：首次 `recognize` 初始化（解压+建 session），成功后缓存；第二次不再加载（spec §10 清单第 6 项依据）
- [ ] 单飞队列：并发 `recognize` 串行执行不崩溃（spec §10 清单第 7 项依据）；引擎加载失败返回带原因错误
- [ ] 真机：中/英/混排样张识别正确；首次 ≤5s、稳态单张 ≤3s（spec §8）

## Comments

### 会话交接（2026-08-16，票据 05/06 已完成后开工本票据）

**当前代码状态**（`bedcode-mobile/src-tauri/src/ocr/`）：mod.rs（契约类型）、engine.rs（trait + 路由 + recognize/engine_status 命令体）、ppocr.rs（骨架：`PpOcrEngine` unit struct，recognize 返回 `not implemented`——本票据填充）、models.rs（状态机 + 惰性解压）、preprocess.rs（`RgbaImage`：校验/灰度/归一化/降采样纯函数，已有单测）。命令/权限/门控全部就位。

**依赖事实**：
- ort **2.0.0-rc.13**（crates.io 无 2.0.0 稳定版），`default-features=false + load-dynamic + ndarray + api-20`，**仅 android target 依赖**（Windows 不编译 ort）——推理代码必须 `#[cfg(target_os = "android")]` 包裹；纯逻辑（排序/bbox/预处理）放非 cfg 函数可单测
- .so：`gen/android/app/src/main/jniLibs/{arm64-v8a,x86_64}/libonnxruntime.so`（1.20.0，不入库；`sh bedcode-mobile/scripts/fetch-ort-android.sh` 重建）
- 模型：`src-tauri/resources/ocr_models/` 三 ONNX（已在 git）
- ort 源码参考：`~/.cargo/registry/src/index.crates.io-*/ort-2.0.0-rc.13/`（API：`ort::init_from(so_path)` / `Session::builder()?.commit_from_file()` / `Tensor::from_array` / `session.run(Vec<Value>) -> Vec<Value>`，以源码为准）

**Android 编译验证**（Windows）：`cd bedcode-mobile && RUST_MIN_STACK=67108864 npx tauri android build --debug`（tauri CLI 注入 NDK env；RUST_MIN_STACK 防 rustc 栈溢出；只编 lib 更快：先 export NDK env 再 `cargo build --target aarch64-linux-android --lib`，env 见 2026-08-16 记忆 E0463 记录）

**真机验证现状**：无在线设备（`adb devices` 为空），且命令有 require_ocr 门控需插件壳（票据 09）——推理正确性验收（中英混排）留 09/10 真机联调；本票据做到 android 编译通过 + 纯逻辑单测。

**PP-OCR 预处理提示**：det 输入为归一化灰度（或 ImageNet mean/std 归一化 RGB，以导出的 ONNX 实际输入要求为准——RapidOCR 的 PP-OCRv4 ONNX det 输入是 1x3xHxW RGB 归一化，rec 是 1x3x48xW 归一化，cls 1x3x48x192；具体数值在实现时用 onnx 元数据/试跑确认）。
